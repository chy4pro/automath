# Qwen L≥3 RIGIDITY pre-chew — Tab B (Problem S, the ν = L−1 layer)

- Conversation: https://chat.qwen.ai/c/962bf4d0-ccea-4a1b-9676-ab2abb83bdda
- Model: Qwen3.8-Max (confirmed switched before dispatch)
- Brief: `prompts/w61_L3RIG_qwen.md` (same S2 template set as Tab A — see that file for
  details).
- Dispatched: 08-18 13:4x CDT (owner-intel qwen_queue driver round 6).
- Harvested: 08-18 14:4x CDT (owner-intel qwen_queue driver round 8). Chrome lease held by
  owner-intel throughout; conversation had already finished generating (composer idle,
  action-icon row visible, verdict line present, page text length static across two checks
  ~3 min apart) — harvested per driver discipline.

## Overall verdict

**PARTIAL** (`VERDICT: PARTIAL`, self-reported) — but the model's own framing is that it
"proves the whole requested Problem S" (the all-universal `B_lo` layer) in full, plus
delivers Problem T (explicit L=3 graph) and a partial classification for general L=3.
Role split per planner TIGHT ruling: owner-w61 holds the decisive-step role and must
re-verify line by line before adoption.

## What it proves

1. **Problem S closed in full** ("the universal-layer theorem"): if every vertex of `B_lo`
   is `B`-universal, then `p=0`, `ν(B_lo)=0`, `c=0`, and (via (MB)) `m̄ ≤ L-1`, hence
   `ν = m̄ ≤ L-1`; moreover every `b ∈ B_lo` has `deg_A(b)=1` with **all these unique
   `A`-neighbours coinciding in a single vertex `a0`**, which is itself adjacent to every
   vertex of `B`. Proved via two new lemmas (Lemma 1: pair-cycle neighbourhood property;
   Lemma 2: degree bounds for low/high vertices) in a 6-step derivation, and explicitly
   checked to hold both at the extreme case `ν = L-1` and for `ν ≤ L-2` (no additional
   rigidity beyond what's already forced). States plainly that (SL) is available but does
   no work in this particular proof.
2. **Problem T**: an explicit admissible `L=3` graph with a *non-universal* low `A`-degree
   multiset `{3,2,1}` (distinguishing it from the all-universal case covered by Problem S).
3. **A second, independently-constructed partial classification for general L=3** (own
   `G1,G2,G3` witnesses plus a local skeleton table), largely convergent with Tab A's
   candidate table despite being generated by an unrelated tab.

## Self-flagged obstruction list (three angles tried, none closes the general L=3 case)

- **Angle 1 (inequality enumeration)**: (MB)/(SL)/(Fb)/(C*) + degree bounds fully enumerate
  local skeletons `(n,p,c,m)`, but do **not** control the number of cross-nonedges between
  `B_lo` and `B_hi` — e.g. row `(n,p,c,m)=(0,1,0,1)` leaves the nonuniversal low vertex free
  to have any number of cross-nonedges without violating the local inequalities.
- **Angle 2 (pair-cycle intersections, Lemma 1)**: strong enough to fully prove the
  universal layer (Problem S), but a low vertex of `A`-degree 2 can be non-adjacent to
  arbitrarily many `B_hi` vertices sharing the same two `A`-neighbours — the intersection
  condition doesn't bound that multiplicity.
- **Angle 3 (direct construction, `G1/G2/G3`)**: proves the universal rows and at least one
  nonuniversal row are genuinely realizable, but for the undecided rows (especially
  `ν(B_lo)=2` or `3`) the real obstruction is again global control of `f(G)=α+1` — ruling
  out induced forests on `α+2` vertices is a hypergraph-level condition on `A`-neighbourhood
  overlaps not captured by any of the listed local facts alone.

## Owner-w61 verification duties on adoption

Line-by-line re-verification of Problem S's 6-step derivation (this is the strongest,
highest-confidence result of the two Q10 tabs — a full theorem, not a partial), independent
check of the Problem-T witness and the `G1/G2/G3` graphs, cross-check against Tab A's
candidate table for consistency, and adversarial review by a NON-Qwen judge before adoption.
Full transcript stays live at the conversation URL above.
