## §7.43 owner-w61 round 28: **THE BRIEF IS REBUILT AGAINST ALL FIVE Q39 DEFECTS PLUS GUARD E'S OWN — and the D1 repair is a WITHDRAWAL plus a SUPPLY, never a restatement of Theorem MB. GUARD E on the rebuilt text: `0` unmatched atoms, down from `3`, with all three deliberate corruptions still caught and TWO NEW FALSE-POSITIVE PROBES silent. Every grading gate now carries a false-positive probe, and the harvest gate's is run on the very report its pre-fix parser VOIDED — the manufactured defect is DEMONSTRATED, not described. The leak probe is now a WRITE-time script and it defected THREE TIMES on its own first runs, every one of them the manufactured-defect species it was written to honour. The bench is still empty and the brief is HELD** (2026-08-23, 05:2x–06:1x CDT)

**Slice.** One brief rebuilt (`+7 492` chars), eight repairs landed, one new builder
guard written, **five** false-positive probes added across four gates, one write-time
leak probe built and run **after** the write, **zero** dispatches, **zero** promotions,
**zero** mathematics touched, and **four defects in my own new checkers**, all four the
same species and all four caught by printing the population.

*(Clock note, once, as in §7.40–§7.42: `date` on this box reads behind the ledger's
CDT convention. Times follow the ledger.)*

### (a) **D1 — the repair is a WITHDRAWAL and a SUPPLY, and it is deliberately not a restatement**

`problems/wowii/w61_r28_build_q40.py` → `.out` → **`prompts/w61_S3_GFAN_r28.md`**
(`120 042` chars, md5 `712ba438…`, `+7 492` vs r26).

sol's D1: **Repair AE1's N4 gloss is "an assertion, not a supplied weakening proof."**
The task book gave exactly two admissible answers — supply the proof or withdraw the
gloss — and **forbade the third**, restating Theorem MB weaker so the gloss comes out
true. **Both admissible answers are taken and the forbidden one is not.**

**Repair AF1 — WITHDRAWN.** Theorem RIG's trailing bound now reads as its source states
it: *"In the **hard core** (i.e. adding the reductio) one has in addition `ν ≤ L − 1`."*
The gloss's words are gone from the brief (`0` occurrences, asserted at build time), and
a withdrawal notice stands at the site saying what was there, who struck it, and why.
**And the draft agreed with the judge all along**, which is the part worth recording: §7.12
B's own *"Scope, stated exactly"* paragraph says **"Only the bound `ν ≤ L−1` needs the hard
core"**, and §7.9's tier list files that bound under *frame + reductio + `τ ≥ 4`*. The brief
had been shipping a **weaker filing than its own source**, for two rounds, in a scope note.

**What the withdrawal costs: nothing.** Every consumer of the bound in this brief is
stated *in the hard core*. sol said so in the same breath as the defect — *"the target
corollary is itself filed in the hard core, so this gap does not furnish a
counterexample"* — and that is why the honest repair is also the cheap one.

**Repair AF2 — SUPPLIED.** **Theorem MB's proof is now printed in Appendix A.1.** It was
the single missing link and it is the link the finding named: Corollary MB1's proof was
already in the brief, so *"the MB proof is not supplied"* was the whole of the
unavailability. The scope question is now **answerable from the file**, and the brief
still **refuses to answer it** — the same refusal that produced last round's finding.
Printed next to it, because a disclosure is worth more than a silence: **Theorem SL's
proof is NOT in this brief and we do not claim it is**; if the scope question needs SL,
*"I could not check it from this file"* is the correct report.

〔**The temptation, named, because it was real.** §7.9's Repair X3(ii) and Repair AA2
*already* record that Theorem MB's proof consumes only frame + reductio and that the
`τ ≥ 4` is inherited from Theorem T3. It would have taken one sentence to quote that
audit into the brief and declare D1 answered. **That would have been the forbidden move
wearing a citation**: X3(ii) is *our own* claim about our own proofs, and importing it as
though it settled the question is exactly how an assertion gets promoted to a supplied
proof. It is not in the brief. The proof is, and the question is left open.〕

### (b) **D2/D3 — the count was right and the clauses were not**

**Repair AF4/AF5.** The AZ bracket at Corollary GFANν-HC gains **(R-c)** *every low
vertex is B-universal* — Theorem RIG's third hypothesis, supplied verbatim by the
corollary's own hypothesis — and **(G-d)** `residue(G) = α(G)` — Theorem GFANν's
reductio, the same clause **(K-a)** already supplied for Theorem K. Both were **always
supplied**; what was false was the bracket's claim to be a complete roll call.

**The bracket's two-round record is now printed in the bracket itself**, because it is
the most useful thing a judge can be told about it: *r24 counted **three** where there
were four (a count defect); r26 counted **four** correctly and omitted two **clauses**.*
**The count is not the claim.**

〔**And GUARD C could not have caught this, which is stated in the build output rather
than left for a judge to notice.** GUARD C asserts *bracket count == cited imports*. On
the r26 brief it **PASSED** — and that brief is the one with two missing clauses. A guard
that grades the number cannot grade the roll call. The clause audit is by hand, it says
so, and no guard is claimed to have replaced it.〕

### (c) **GUARD E's own finding — provenance, and the second half nobody reported**

**Repair AF3.** §7.42 (d) found that the brief printed, *inside Theorem SL's statement
block*, an inequality that lives inside **Theorem MB's proof**. Re-reading the source for
this repair turned up **the other half of the same defect, one layer further in**:

〔**(LOW3) is a labelled form of Theorem LOW, not of Theorem SL, and it has no `− 1`.**
§7.5 states **(LOW3)** as `Σ_{b∈B_lo} deg_A(b) + m̄ ≤ 2L + ν(B_lo)`. The `− 1` is Theorem
SL's one-unit strengthening. So the brief's *"its (LOW3) form"* mis-attributed the label
**and** the owner: the shape it printed is neither SL's statement nor LOW's (LOW3), but
SL-strengthened-LOW3, and the draft writes it out only inside Theorem MB's proof.
GUARD E found the site; reading the site found the label.〕

The repair does **not** touch the mathematics. Theorem SL's block now carries exactly the
two forms its source block carries — including `Σ deg_A(b) + e(B_lo) + m̄ ≤ L(L+3)/2 − 1`,
which the r26 brief had **dropped** and which only the REVERSE direction of GUARD E could
see. The `2L + ν(B_lo) − 1` shape appears where the source puts it, inside MB's supplied
proof. **And the one-line substitution connecting them is now printed rather than assumed**
— `e(B_lo) = C(L,2) − ν(B_lo)` with `L(L+3)/2 − C(L,2) = 2L` — and flagged to the judge as
*a claim of ours, load-bearing for Theorem MB, MATHEMATICS if wrong*. A brief that prints
its own derivation is checkable; one that says "equivalently" is not.

**Repair AF6.** A.1 had regularised §7.2 B's `deg v` to `deg(v)`. Fixed **on the brief
side**. The other fix — one more entry in GUARD E's transform table — is what RULING CE
forbids, and it would have made the guard blind to the next one.

**Repairs AF7/AF8** close D4 and D5. D5 is worth its own line: *"the pasted diff cannot
establish that the programs were independently written."* **That is RULING CD arriving
from the judge's side of the table, about our file.** The claim is **withdrawn at all
three sites** rather than defended, and what the diff does show is left standing.

### (d) **GUARD B and GUARD E on the rebuilt brief**

`problems/wowii/w61_r28_guardE.py` → `.out`.

| | r26 as shipped | **r28 rebuilt** |
|---|---|---|
| atoms matched **in block** | `39` | **`43`** |
| matched **elsewhere** (provenance) | `10` | **`9`** |
| **NO MATCH ANYWHERE** (defects) | **`3`** | **`0`** |
| three deliberate corruptions | all detected | **all detected** |

GUARD B (inside the build): **`18/18` covered names carry a statement block**, `0` misses.
GUARD A `0` leaks, GUARD C count `4 == 4`, GUARD D `0` bounty-shaped controls.

### (e) **RULING CI executed: a FALSE-POSITIVE probe on every grading gate**

> *"A miss costs us a round; a manufactured defect costs us a correct result and slanders
> the judge that produced it."*

Liveness proves a gate **can say NO**. It says nothing about whether the gate says NO to
things it has no business objecting to. **Five false-positive probes are added, and the
harvest gate's are run on the very report its pre-fix parser voided.**

`problems/wowii/w61_r28_fpprobe.py` → `.out`. Six inputs, three that must **PASS** and
three that must **VOID**, run through **both** parsers side by side:

| probe | pre-fix parser | fixed parser |
|---|---|---|
| **FP-1** the Q39 report as harvested (**known `7/7` EXACT**) | **VOID — MANUFACTURED** | **PASS** `7/7` |
| **FP-2** same, H2 term-list label cosmetically relabelled | **VOID — MANUFACTURED** | **PASS** `7/7` |
| **FP-3** same, H3 answered `CANNOT COMPUTE` (the hatch) | **VOID — MANUFACTURED** | **PASS** `6/7` |
| **FN-1** H2 total corrupted | VOID | **VOID** |
| **FN-2** one H2 term corrupted | VOID | **VOID** |
| **FN-3** H5 step count corrupted | VOID | **VOID** |

**The pre-fix parser is kept and run, not described.** §7.42 (e) recorded that it
manufactured a VOID; this reproduces it on demand, on a real report, and shows the fix
closing it without opening a miss. GUARD E carries two of its own (whitespace reflow
inside a block; a Unicode/ASCII difference the declared transform table exists to absorb)
— **both SILENT**. GUARD A, B, D and the new GUARD F carry theirs inside the build.

**The limit is printed in the probe's own output**: these test the **parser**, not the
**key**. A gate with wrong key values grades every report wrongly and passes every probe
here. The key is guarded separately, by re-reading each value out of the key **file**.

### (f) **GUARD F — the D1 species as a class, and it fired on my own repair text**

New builder guard. **An UNSUPPLIED WEAKENING ASSERTION**: a sentence claiming an import's
proof consumes less than its statement is filed under, where **that import's proof is not
printed in the brief**. That is D1's shape, stated as a class that names no lemma and no
phrase from the struck sentence.

* **Liveness**: run on the r26 base it fires on the N4 gloss, at both of its sites.
* **It fired on the r28 draft too — on a sentence I had just written.** My own AF2
  explanatory paragraph restated the withdrawn claim in the indicative in the course of
  reporting it. **A repair note that re-asserts the thing it is repairing is the same
  defect with better manners.** Rewritten; the guard is silent; the incident is here.
* **And its first run MANUFACTURED one.** It flagged Theorem RIG's own *"Scope, stated
  exactly"* paragraph — *"(a)–(c) and (e) need only `A` maximum plus **Lemma 4**"* —
  reading the **listed hypothesis** `Lemma 4` as if the sentence were a claim about
  *Lemma 4's* proof. Root fix is **structural, not a word list**: in `X uses only Y`, names
  **before** the predicate are subjects (their proofs are graded) and names **after** it
  are objects (they are not). Both false-positive probes for GUARD F are built from these
  two shapes, so the next regression is caught by the guard's own suite.

### (g) **The leak probe belongs to the WRITE — and it defected three times proving it**

`problems/wowii/w61_r28_leakprobe.py` → `.out`. RULING CI's second half, in the words the
cert adopted: **a check that runs before the thing it guards against cannot guard it.**
The r27 probe lived in the *build*; §7.42 (c) then printed the new key's `H2` answer into
the draft two hours later. This one is a standalone script whose job is to run **after a
write**, and it is run again below, **after §7.43 was appended**.

**RULING CH, executed rather than cited.** The corpus is **`426` files** — the draft,
**`35`** shipped briefs, **`194`** ledger and planner files, **`197`** `problems/wowii`
artifacts — not an outing count and not a spot check. **Population `59` entries, printed
as digit masks. Discriminating hits: `0`.** `ν ≤ 11` burned, **`ν = 12` live**, unchanged.

〔**Three defects in this one probe, all on first runs, all the same species — the
manufactured defect — in the probe written to honour the ruling against manufactured
defects.** **(L-1)** the population was *every integer of two or more digits*, so
two-digit values that collide with ordinary prose across `426` files produced ~60 "hits".
**(L-2)** the population's boundary was found by searching for the name `H1`, which swept
in the key file's own `--- controls ---` block of **published** `ν ≤ 10` numbers.
**(L-3)** it swept in the **intermediate factors** of `H2`'s computation, which are the
partition numbers `p(k)` — published constants that occur all over this corpus for good
reasons — and reported one of them as a burned key value. Each fix is a **class rule with
a printed population**: *only ≥ 4 digits is discriminating*; *bound the population by the
file's own section markers, not by a name*; *the value of a `p(k)=` factor is a published
constant, not a key answer*. **Direction of error in all three: against ourselves** — each
would have forced a needless re-key, none would have shipped a burned key. That is the
safer direction and it is still three instances in one file.〕

### (h) **What did NOT happen — and the brief is HELD**

**No dispatch.** The rebuilt brief `prompts/w61_S3_GFAN_r28.md` is built, guarded and
**not sent**. **The family bench is empty**; the `OR_KEY` `limit: 0` cap is with the user,
where it belongs, and **this round did not route around it.** GUARD E's own output labels
the run *"NOT YET SENT — the bench is empty"* rather than inheriting r27's *"as sent to
Q39"*, which is the stale-address species RULING BB exists for and which I introduced by
copying the file.

**A brief held for want of a seat is a better position than a brief spent on a re-read.**
Corollary GFANν-HC's next family round must be a **family this text has not seen**; the
only judges qualified on the current harness have all engaged this line already, and a
`GAP` from a disqualified seat buys the same zero Q39 bought.

**No promotion.** Corollary GFANν-HC stays at **`1` family (Q35, Meta)**. **No address
minted** — `R-24` remains the lowest free row. **No mathematics touched**: §7.13 D
untouched for the fourth round running, no bounded weakening altered, no statement
re-tiered. **Repairs AF1–AF8 are all repairs to the BRIEF**, not to the draft; the one
draft-facing finding of this round — that (LOW3) is Theorem LOW's label, not Theorem SL's
— is a **naming** fact already true in §7.5 and is recorded here, not edited into §7.6.

### (i) **Named next round**

1. **The bench.** Nothing on this line promotes without a fresh family. If the `OR_KEY`
   cap lifts, `w61_S3_GFAN_r28.md` dispatches **as built**, graded against the `ν = 12`
   key, with the leak probe re-run at write time first.
2. **The open J-SCOPE question is now open ON THE RECORD**, not closed by a gloss: does
   Theorem MB's proof consume `τ ≥ 4`? The brief supplies MB's proof and refuses to
   answer. §7.9's X3(ii)/AA2 audit says no; **that audit is ours and has never been
   judged**, and turning it into a brief-level claim is the move this round declined.
3. **Theorem SL's proof is the next supply.** It is the one link of the MB chain still
   absent from the brief, and it is named in the brief as absent.
4. **Unchanged and still queued:** the §7.25 own-attack family (Lemma C1-A, C1-A′,
   Proposition C1-B, Corollary C1-C, Theorem C1-2) at **0** rounds; Conjecture C1 for
   `k ≥ 3`; the configuration/tier import sweep (§7.38 (f)) as a pre-dispatch gate.

**And the limit that rides with this round's headline.** Eight repairs landed and **not
one of them makes the target corollary more likely to be true.** They make the brief
*checkable*: a withdrawn assertion is not a proof, a supplied proof is not a verdict, and
a complete roll call is not a correct one. **The r25 GAP's ground is gone, the r27 GAP's
ground is repaired, and Corollary GFANν-HC still stands at one family** — because the only
thing that moves that number is a second judge this text has never met, and the bench is
empty. Everything in this section is preparation for a round that has not been bought yet,
and saying so is cheaper than discovering it later.

### (j) **Round accounting**

| | |
|---|---|
| brief rebuilt | `prompts/w61_S3_GFAN_r28.md`, `120 042` chars, md5 `712ba438…`, `+7 492` vs r26 |
| repairs landed | **8** — AF1 withdraw N4 · AF2 supply MB's proof · AF3 SL provenance + printed substitution · AF4 R-c · AF5 G-d · AF6 `deg v` verbatim · AF7 RIG-1 over-cite · AF8 "independently written" withdrawn ×3 |
| D1 answered by | **withdrawal + supply.** Theorem MB **NOT** restated weaker; the scope question left **unanswered** for the judge, as in r26 |
| draft agreed with the judge | §7.12 B *"Only the bound `ν ≤ L−1` needs the hard core"* — the brief had been shipping a **weaker filing than its own source** |
| GUARD E on the rebuild | **`43` in-block / `9` provenance / `0` NO-MATCH** (was `39/10/3`); 3 corruptions caught; **2 false-positive probes SILENT** |
| GUARD B / A / C / D | `18/18` statements · `0` leaks · count `4 == 4` · `0` bounty controls |
| GUARD F (new) | unsupplied-weakening class; fires on the r26 gloss, silent on r28, **2** false-positive probes |
| false-positive probes added | **5** across four gates, plus the harvest gate's **3**, run against **both** parsers |
| harvest gate FP result | pre-fix parser **manufactures a VOID on all three** admissible inputs; fixed parser **PASS/PASS/PASS**, misses nothing |
| leak probe | **WRITE-time**, `426` files, `59`-entry population, **`0` discriminating hits**; `ν ≤ 11` burned, `ν = 12` live |
| own-checker defects | **4** — GUARD F manufactured one on RIG's scope paragraph; the leak probe manufactured three (L-1/L-2/L-3). **All four against ourselves**, all four caught by printing the population |
| dispatches | **none.** Bench empty, `OR_KEY` cap with the user, **not routed around** |
| promotions | **none.** Corollary GFANν-HC stays at `1` family; `R-24` still the lowest free row |
| repairs to mathematics | **none.** §7.13 D untouched; no bounded weakening altered; nothing re-tiered |
| lease | none taken |
