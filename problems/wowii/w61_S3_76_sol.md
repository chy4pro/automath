VERDICT: PARTIAL — No MATHEMATICS defect was found and every conclusion in the chain survives; the only gap is BOOKKEEPING, namely that Corollary SL-HC's unused `tau >= 4` rider is attributed to an unavailable Theorem T3, together with the explicitly acknowledged non-minimal tier assignments.
TEXT VERSION REVIEWED: w61_S3_76_sol (Q26).

## 1. Refutation log

I searched before accepting any proof. My implementation is
`problems/wowii/w61_S3_76_sol_check.py`; its retained summary is
`problems/wowii/w61_S3_76_sol_check.out`. It imports no project checker and reads no
project file. I did not read any path prohibited by the brief.

The common search box was:

* every unlabeled graph on 2 through 7 vertices in NetworkX's graph atlas, connected
  or disconnected;
* 216 independently generated `G(n,p)` graphs, with `n=8,9,10`,
  `p in {0.18,0.35,0.55,0.78}`, and 18 fixed seeds for each `(n,p)`;
* the cycles `C3,...,C9` and cliques `K2,...,K7` again as named controls;
* every maximum independent set `A` of every graph, found by exhaustive subset
  enumeration, never merely a maximal independent set;
* for every reductio instance, the canonical descending sort and five seeded,
  freshly randomized tie orders at every HH step.

In total this was 1,480 graphs, 4,627 `(G,A)` instances, 849 reductio graphs,
3,447 reductio `(G,A)` instances, and 20,682 labelled trajectories. There were
1,159 connected graphs. The box contained eight hard-core-frame graphs (nine
`(G,A)` instances), but no graph satisfying frame plus `residue=alpha`; therefore
direct tests of the composite hard-core statements were vacuous. I did not disguise
that absence as positive numerical evidence.

### Lemma HI and Theorem LOW

Attack: on all 20,682 reductio trajectories I compared `B_hi` with the labelled
high-head set, checked that the high/low head positions exhaust all `tau` positions,
checked the position identity, and evaluated LOW1, LOW2, and LOW3 independently.
I included disconnected reductio graphs because these statements do not assume
connectivity. I hit `L=0,1,2,tau`, `p=0`, `tau=2,3,4`, `nu=0,1`, `Delta=tau`,
`Delta=tau+1`, and both signs of `B_lo^+` emptiness. Result: no counterexample and
no failed intermediate assertion.

### Theorem SL

Attack: on every eligible trajectory (`tau>=2`, `L>=1`) I tested the strict
one-unit improvement, under all six tie orders. No slack-zero instance occurred.
The observed selected slack counts were: `L=0`: 648 trajectories at zero;
`L=1`: 1,068 at one; `L=2`: 192 at one, 1,572 at two, 672 at three;
the observed minima for `L=3,4,5` were respectively `2,2,3`. These are finite-box
observations, not a proof. The requested `i0=1` boundary occurs at `tau=1` (`K2`);
for `tau>=2` Step 1 proves that it cannot occur under the assumed slack-zero branch.
The `i0=tau` boundary occurred, for example, on `K3` with a one-vertex maximum
independent set. No counterexample was found.

### Theorem MB

Attack: no complete hard-core instance existed in the box, so the displayed MB
inequality had no direct non-vacuous test. I therefore attacked the independent
frame-side ingredient on all nine hard-core-frame `(G,A)` instances and every
admissible `(F-b)` type pair: the three floors, the inclusion into `B_lo^+`, and
the combined degree count were tested separately. There were nine resulting
degree-count instances; all passed, and all nine happened to have count slack zero.
The reductio-side ingredient, Theorem SL in LOW3 form with the `-1`, was tested in
the preceding search. No ingredient-level counterexample was found.

### Corollary L1-short

Attack: I searched every hard-core candidate with `L=1`. There were no complete
hard-core candidates in the box. Reductio-only `L=1` was non-vacuous (1,068
trajectories, including examples with `tau>=2`) and always had SL slack one, but
those examples do not satisfy the structural hypotheses and were not misused as
tests of L1-short. No counterexample was found; the composite numerical test was
vacuous.

### Corollary MB1

Attack: I searched hard-core candidates in which every low vertex is B-universal,
including the `B_lo^+=empty` boundary. Again there were no complete hard-core
candidates. Its degree-count and SL ingredients passed separately. No
counterexample was found; the direct composite search was vacuous.

### Proposition L1 and Corollary L1-prime

Attack: these were searched because Corollary SL-HC invokes them and J-SCOPE asks
for them. No hard-core `L=1` instance occurred. The reductio-only controls are not
witnesses for these structural statements. No counterexample was found; direct
testing was vacuous.

### Proposition L2

Attack: I searched separately for failure of (a), (b), (c), (d), and (e) on every
hard-core candidate with `L=2`, under every `A` and tie order. There was no complete
hard-core candidate. Frame examples with `L=2` were used only to attack the
degree-count premise, because SL is unavailable there. No counterexample was
found; the composite test was vacuous.

### Corollary SL-HC

Attack: I searched for a complete hard-core instance with `L=0` or `L=1`; none
occurred. The two branches were also attacked at their separate reductio and frame
ingredients. No counterexample was found. The mathematical conclusion was then
checked from the proof, below.

### Scope assignment

Attack: `C5` was used to try to force a hidden frame assumption into LOW or SL.
It has edge set
`{01,04,12,23,34}`, is connected, has `alpha=residue=2`, `tau=3`, diameter 2,
and `f=4 != alpha+1`; all five maximum independent sets and all tie orders passed
HI, LOW, and SL. Thus the proposed widening of those statements outside the frame
survived. Conversely, the frame control in T12 below has `residue<alpha`, so it
separates the frame facts from the reductio facts. No under-hypothesised theorem
was found. Six statements are filed with an unnecessary `tau>=4`, as the text
itself acknowledges.

## 2. Joint table

| Joint | Individual verdict | Verification |
|---|---|---|
| J-LOW / Lemma HI | CLEAN | `deg(a)<=tau` puts every degree-`>=tau+1` vertex in `B`; Lemma S makes each such vertex a head. Conversely a high head is such a vertex, hence `I_hi=B_hi`. Every one of the `tau` head positions is then high or low and `|I_lo|=tau-p=L`. |
| J-LOW / LOW1 | CLEAN | Substitution of the exact DICH(b) values and DICH(c) bounds is sign-correct. The bracket is `(i-1)+(tau-i+2)=tau+1`, so the position sum is exactly `-C(tau,2)+L(tau+1)`, independent of positions. With `e_B=C(tau,2)-nu`, LOW1 follows. |
| J-LOW / LOW2 | CLEAN | Expanding gives `sum deg_A +2e_lo+e_cross + [C(L,2)-e_lo]+[Lp-e_cross]+mbar`; `e_cross` cancels and one `e_lo` remains. Since `tau=L+p`, `L(tau+1)-C(L,2)-Lp=L(L+3)/2`. |
| J-LOW / LOW3 | CLEAN | Replacing `e(B_lo)` by `C(L,2)-nu(B_lo)` gives `sum deg_A+mbar <= 2L+nu(B_lo)`. The rewrites remain valid at `L=0,1,tau`; direct evaluation also covered those boundaries. |
| J-SL / Step 1 | CLEAN | If `i0=1` and there is a later high head, its original degree exceeds the first head's `g<=tau`, contradicting `D1=Delta`. If all heads are low then `i0=tau>=2`. |
| J-SL / Step 2 | CLEAN | Before deletion, `x` is decremented in exactly `i0-1-|E|` steps, so equality gives `|E|=tau+1-g>=1`. For `j=max E`, step `j` does not decrement `x` and every step `j+1,...,i0-1` does, yielding `v_j=tau-j+1` with no off-by-one error. |
| J-SL / Step 3 | CLEAN | A non-increasing prefix dominates every outside entry even when values tie; membership is irrelevant. Thus every block value is at least `v_j`. A survivor is at most the F3 threshold, hence equals it, and must be decremented in every one of the remaining `tau-j+1` steps. The tie case is safe. |
| J-SL / Step 4 | CLEAN | Entries present after the step-`j` head partition into later heads and survivors. There are exactly `tau-j-ell_j` later high heads and DICH(b) puts all in the block. `lambda_j` is the actual low-later-head count in the block; because `x` is one of the `ell_j` low later heads and escapes, `lambda_j<=ell_j-1`. The equality for `D_j` precedes, and is compatible with, this upper bound. |
| J-SL / Step 5 | CLEAN | After `i0` every head is high, so all `tau-i0` lie in `block_i0`; Step 3 puts the `s_j` survivors there too. Heads and survivors are disjoint, so `D_i0>=tau-i0+s_j`; equality at the low head gives `s_j<=2`. |
| J-SL / Step 6 | CLEAN | `j` is an HH step index, hence necessarily a head position. Step 4 with `lambda_j<=ell_j-1` and Step 5 gives `D_j<=tau-j+1`. If high, DICH(b) gives `D_j>=tau-j+2`; if low, global slack equality gives equality to `tau-j+2`. Both cases contradict the upper bound. |
| J-MB | CLEAN | The low vertices are partitioned as universal (floor 1), non-universal outside the type union (floor 2), and low vertices in the type union (floor 3). For a low `b` in one type, the other occurring type is nonempty, disjoint, and has no cross B-edge, so `b` has a B-nonneighbour and lies in `B_lo^+`; hence `c<=|B_lo^+|`. No vertex is omitted or double-counted. Substitution into SL's LOW3 form is `sum deg_A+mbar <=2L+nu_lo-1`; rearrangement places the `-1` on the right and gives MB. |
| J-L1s | CLEAN | `nu(B_lo)=0` for a singleton. The three terms in MB are cardinalities/counts and nonnegative, so all vanish. With only one low vertex, B-universality means every B-nonedge lies wholly in `B_hi`, hence `nu=mbar=0`; this is not circular, because `mbar=0` came first from MB. R1 then contradicts cliquehood. |
| J-MB1 | CLEAN | Low universality eliminates nonedges within low and across low/high, so `nu=mbar`. A low vertex in `T1 union T2` would have a nonneighbour in the nonempty other type by (F-b), so `c=0`. MB gives `mbar<=L-1`, while R1 gives `nu>=1`; therefore `L>=nu+1>=2`. |
| J-L2(a) | CLEAN | If the two low vertices are nonadjacent, both are in `B_lo^+`; MB forces `c=mbar=0`. Thus both occurring types lie in `B_hi`, which is a clique, contradicting their required cross nonedges. |
| J-L2(b) | CLEAN | After (a), `nu(B_lo)=0` and MB's right side is one. If any low vertex is in `B_lo^+`, the inequality itself first forces `c=mbar=0`; only then is the same (F-b) contradiction invoked. No missing premise is borrowed from (a)'s old branch. |
| J-L2(c) | CLEAN | With `L=2`, `B_lo^+=empty`, `c=0`, `mbar=1`, and `nu_lo=0`, the MB degree count gives the lower bound 2 and SL gives the upper bound `4-1-1=2`. They apply simultaneously to the same hypothetical graph, so the squeeze is valid. If their equality cases had been incompatible, that would rule out the hypothetical graph rather than invalidate the conditional conclusion. Lemma 4 then makes the two unique A-neighbours coincide, and B-universality propagates that vertex to all of B. |
| J-L2(d) | CLEAN | B-universality puts every B-nonedge in `B_hi`; `mbar<=1` and R1 yield `nu=mbar=1`. Every cross pair of the two nonempty disjoint types must be this unique nonedge, forcing singleton types, and C-star supplies A-degree at least 3 at their vertices. |
| J-L2(e) | CLEAN | Each low vertex has B-degree `tau-1` and A-degree 1, hence total degree `tau`. Therefore slack is `2(tau+1)-(2tau+1)=1`. This also independently proves the tightness rider. |
| J-SLHC | CLEAN conclusion / PARTIAL rider | The `L=0` branch is K plus R1. The long `L=1` branch (Proposition L1, L1-prime, SL) and the short MB branch are both valid under frame plus reductio. They have the same effective hypotheses; the short proof does not need Proposition L1. SL needs only `tau>=2`, which follows directly from diameter 4. The parenthetical `tau>=4` is unused and its cited supplier T3 is not available in the appendix. |

### J-SCOPE, one line per statement

| Statement | Tier needed by its own proof | Filed tier and verdict |
|---|---|---|
| Lemma HI | reductio only (plus the standing choice of maximum `A`) | reductio only; exact |
| Theorem LOW | reductio only | reductio only; exact. `C5` is a non-frame witness to non-vacuity |
| Theorem SL | reductio only, with its explicit `tau>=2` | reductio only; exact provided the explicit numerical rider is retained |
| Theorem MB | hard-core frame plus reductio; no separate use of `tau>=4` or non-forestness | filed hard core; valid but over-hypothesised by `tau>=4` |
| Corollary L1-short | frame plus reductio; `tau>=2` follows from diameter 4 | filed hard core; valid but over-hypothesised by `tau>=4` |
| Corollary MB1 | frame plus reductio; no use of `tau>=4` | filed hard core; valid but over-hypothesised by `tau>=4` |
| Proposition L1 | frame plus reductio; no use of `tau>=4` | filed hard core; valid but over-hypothesised by `tau>=4` |
| Corollary L1-prime | exactly the hypotheses needed by Proposition L1 | filed hard core; valid but over-hypothesised by `tau>=4` |
| Proposition L2 | frame plus reductio; no use of `tau>=4` | filed hard core; valid but over-hypothesised by `tau>=4` |
| Corollary SL-HC | frame plus reductio and `tau>=2`; the latter follows from diameter 4 | filed hard core; valid but `tau>=4` is unnecessary and not locally supplied |

There is no under-hypothesised entry in this list. The six over-hypotheses identified
in the author's scope note are real non-minimalities, not invalid statements. The
same observation also applies to SL-HC. Theorem LOW and Theorem SL make no silent
use of connectedness, diameter 4, `f=alpha+1`, non-forestness, or `tau>=4`.

## 3. Defects

### D1 — unavailable and unused `tau>=4` rider

* Quoted line: "`note tau >= 4 in the hard core`" in Corollary SL-HC, together
  with "Together with Theorem T3 (`tau<=3` fully solved)" immediately afterward.
* Why it fails: T3's statement and proof are not supplied, it is not in the
  certified-out-of-scope list, and the read restriction forbids looking elsewhere.
  Consequently this appendix does not establish the rider as a derived fact for
  the parenthetical definition of hard core printed in the corollary. The proof
  does not need it: SL asks for `tau>=2`.
* Smallest configuration exposing the availability distinction: the frame control
  `E={02,13,24,25,34,35}`, `A={0,1,4,5}`, has connectedness first, then maximum
  `A` with `alpha=4`, diameter 4, `f=5=alpha+1`, and is non-forest, but has
  `tau=2`. It is not a counterexample to SL-HC because `residue=3<alpha`; it shows
  that the frame does not itself supply `tau>=4`. No full-hard-core witness was
  found.
* Repairable: yes, editorially: the proof may cite the elementary consequence
  `diameter 4 => tau>=2` and omit the `tau>=4` aside. I am not proposing a new
  mathematical statement.
* Does the CONCLUSION survive: yes, unchanged.
* Classification: **BOOKKEEPING**.

### D2 — hard-core filing is not the weakest proof scope

* Quoted line: "hard core ... Theorem MB, Proposition L1/L2, Corollary L1-prime,
  Corollary MB1, Corollaries SL-HC, L1-short".
* Why it fails as a minimal hypothesis assignment: each named proof uses frame plus
  reductio, while none consumes the added `tau>=4`. The appendix itself accurately
  acknowledges six of these cases; SL-HC has the same non-minimality.
* Smallest configuration: no witness to frame-plus-reductio below `tau=4` was found,
  and Theorem 6 rules out `tau<=2`; hence the mismatch is established by proof-use
  audit, not by a graph counterexample. The 6-vertex frame control above verifies
  that `tau>=4` is not a frame fact.
* Repairable: yes, by recording the effective scope as frame plus reductio and
  separately recording the downstream call-site scope. No theorem text needs to
  change.
* Does the CONCLUSION survive: yes, for every statement.
* Classification: **BOOKKEEPING**.

No **MATHEMATICS** defect was found.

## 4. Repair-species probe: Statement riders are separate proof obligations

* "Theorem K is the case `L=0`" is proved, not merely inherited: LOW1 becomes
  `nu<=0`; since `nu` is a count, `nu=0` and `B` is a clique. The hypotheses agree
  with K's reductio-only hypotheses.
* Corollary MB1's closing `>=2` is justified by `nu>=1` from R1 and
  `nu=mbar<=L-1`, so `L>=nu+1>=2`.
* Proposition L2(e)'s tightness is separately proved by exact degree and slack
  arithmetic; it does not rely on the word "tight" in SL.
* "Where this leaves the line" correctly states a necessary residual shape. It
  does not prove that `Fan(tau,2)` exists. The later formula `slack=L-1` is a valid
  conditional calculation for the described fan data, not an existence proof.
* The numerical bullet title "LOW1 is tight exactly at `L=0`" is too categorical
  if detached from its following corpus qualifiers. In context the next sentences
  say "in the whole corpus" and "measurably", so I treat it as an empirical rider,
  not a theorem. It is not used in a proof.
* The only unsupported rider is the `tau>=4` aside described in D1.

## 5. Repair-species probe: Scope-widening contagion

I reread each sentence following the widening of HI, LOW, and SL to reductio-only
scope. HI uses only `deg(a)<=tau`, Lemma S, and the reductio head count. LOW uses
only Lemma 1, HI, DICH, and edge-count identities. SL uses only LOW's exact slack
identity, F3, DICH, and `tau>=2`. In particular, Step 5's word "high" refers to
the degree split, not to a frame property. The equivalent LOW2 form of SL follows
by exactly the same algebra as LOW2 with one subtracted on the right; it does not
import structure.

The `C5` control makes this widening non-vacuous while violating every special
frame equality relevant here (`diameter=2`, `f=4`, `alpha+1=3`). All five maximum
independent sets and all tie orders passed.

The following section G explicitly narrows to the hard core before introducing
MB. No MB or L2 sentence silently leaks back to reductio-only scope. The opposite
mismatch is over-hypothesis: frame-plus-reductio statements are filed at hard core
with `tau>=4`. That is safe for truth but non-minimal.

## 6. Repair-species probe: Box-vs-branch quantification artifact

The graph family is infinite; none of the displayed histograms can certify it.

* `[SLACK]`: the numbers are finite-corpus results. The prose eventually says
  "whole corpus", "measurably", and calls larger-`L` behavior a lead. The bold
  lead sentence "tight exactly" should not be quoted without that qualifier.
* `[HARDCORE]`: the text explicitly says reductio was not imposed and explains
  that the histogram describes a target regime, not evidence for a theorem.
* `MB degree-count slack histogram`: the addendum states the finite parameter
  box and gives 288 `L>=1` instances. Its conclusion is only that the ingredient
  is tight in 180 of those 288 instances; no universal claim is inferred.

Closed-form proofs replace the counts for LOW, SL, MB, and L2. I found no theorem
whose quantifier is improperly upgraded from a computational box. The bold LOW
sentence is a wording hazard only, and no conclusion depends on it.

## 7. T12 controls

### T12.1 Counterfactual availability

Control R (reductio only) is `C5` with the edge list above. Basic checks first:
it is connected; each of
`{0,2},{0,3},{1,3},{1,4},{2,4}` is a maximum independent set of size 2;
`residue=2=alpha`; hence `tau=3` and `s=tau`. The statements advertised as
available here have their hypotheses: Lemma S/HI, LOW, and DICH have reductio;
SL additionally has `tau>=2`. LOW and SL actually execute. Lemma T is also
available from `s=tau` but never executes in the reviewed LOW-to-SL proof; it is
an explicit example of an available lemma that is not used.

Control F (hard-core frame) has
`V={0,1,2,3,4,5}` and `E={02,13,24,25,34,35}`. Basic checks first: it is
connected; `A={0,1,4,5}` is maximum, not just maximal (`alpha=4`); its diameter
is 4; it contains the 4-cycle `2-4-3-5-2`, so it is non-forest; and
`f=5=alpha+1`. With `B={2,3}`, the occurring types `{2}` and `{3}` form an F-b
pair. Vertices 2 and 3 are nonadjacent, have the two common A-neighbours 4 and 5,
and each has A-degree 3, so the invoked forms of Lemma 4, F-b, R1, and C-star all
hold. The graph has `residue=3<alpha=4`, so reductio lemmas are correctly
unavailable. This is also a counterfactual check that the frame alone does not
supply `tau>=4`.

No full-hard-core control occurred in the search. Thus I could not validate
"every lemma available in a hard-core branch" on an actual control. In particular,
Theorem T3 is treated as supplying `tau>=4` but is absent and never executes in
SL-HC; its hypotheses and conclusion could not be checked.

### T12.2 Witness validation

The two graph witnesses above were validated in class-definition order. Control F:
connected; `A` exhaustively maximum; diameter 4; `f=alpha+1`; non-forest;
then `residue=3`, showing deliberately that it is frame-only. Control R: connected;
all maximum independent sets exhaustively listed; then `residue=alpha`; its
diameter and forest number deliberately exclude it from the frame. The `L=1`,
`tau=2` reductio boundary used computationally is the graph
`E={03,12,13,23}`, `A={0,1}`; exhaustive checking gives `alpha=2`,
`residue=2`, `B={2,3}`, `L=1`, and slack 1. It is cited only for SL, not for a
hard-core claim.

### T12.3 Non-vacuity

I propose no repaired mathematical statement, so no new non-vacuity obligation is
created. LOW and SL are non-vacuous on Control R and on thousands of searched
instances. The frame ingredients behind MB are non-vacuous on Control F and eight
frame graphs total. The composite hard-core statements MB, L1-short, MB1, L1/L1-prime,
L2, and SL-HC may be vacuous as graph-family statements: no instance of all their
hypotheses occurred in this box, and proving global emptiness is the surrounding
project's goal. Their conditional proofs were therefore audited symbolically, not
certified by instances.

## 8. Trajectories and computations

I wrote `residue_seq` directly from the supplied specification: sort descending;
if the head is zero return the current length; otherwise remove the head, apply
Nat-truncated subtraction to the first `D` tail entries using split-at truncation,
re-sort, and recurse. The labelled version retains vertex names, the head, block,
start-of-step values, and survivors. Random tie orders are regenerated independently
inside each equal-value group at each step.

Calibration, performed before trusting any theorem check:

```text
K2: residue=1, expected=1, OK
C3: residue=1, expected=1, OK
C4: residue=2, expected=2, OK
C5: residue=2, expected=2, OK
C6: residue=2, expected=2, OK
C7: residue=3, expected=3, OK
C8: residue=3, expected=3, OK
C9: residue=3, expected=3, OK
```

Process edge controls:

```text
2K2 degrees [1,1,1,1] -> heads [1,1], residue 2
E4  degrees [0,0,0,0] -> heads [], residue 4
nongraphical [3,3,1]   -> heads [3,2], residue 1
nongraphical [5,1]     -> heads [5], residue 1
nongraphical [4,4,1,0] -> heads [4,3], residue 2
```

The nongraphical sequences exercise split-at truncation and Nat subtraction only;
I did not apply graphical lemmas such as `sum D_i=m` to them. For graph inputs I
checked `residue=n-s` and `sum D_i=m`. I checked HI set equality, all three LOW
forms, the position collapse, SL, Lemma 4's invoked pair floors, F-b existence,
C-star, the three MB subclaims, the MB degree count, and—where their full hypotheses
existed—the composite statements. Total recorded failures: zero.

## 9. What I could not check

* I assumed the supplied external facts: Havel-Hakimi preserves graphicality and
  the Favaron-Maheo-Sacle bound `residue<=alpha`. I did not verify bibliographic
  attribution.
* By instruction, I did not referee the proof of Lemma 4 or the already certified
  Lemma S/F3-prime/DICH/Z-plus/K chain. I did check the exact forms used at each
  reviewed call site.
* Theorem T3 was neither supplied nor declared an available certified background
  theorem. I could not check it without violating the read restriction.
* The finite search is not exhaustive beyond seven vertices. The random box through
  ten vertices is falsification evidence only.
* Because the box contained no frame-plus-reductio graph, the hard-core composite
  statements had no direct non-vacuous computational test. Their ingredients and
  proofs were checked separately.

## 10. Out-of-scope observations

The `tau>=4` rider is unnecessary for Corollary SL-HC. A connected graph with a
vertex cover of size 0 is edgeless, and a connected graph with a vertex cover of
size 1 is a star (possibly just an edge), hence has diameter at most 2. Therefore
diameter 4 already gives `tau>=2`, exactly the numerical hypothesis Theorem SL
uses. This observation repairs the proof's availability without relying on T3 and
without changing any conclusion.
