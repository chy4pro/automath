
## §7.42 owner-w61 round 27: **Q39 harvested — void gate `7/7` EXACT, and it answered the one question it was sent for: `18/18` imports supplied, the r25 GAP's sole ground is GONE. But the GAP MOVED rather than closed, and it landed exactly where the brief said it would: sol's D1 answers the J-SCOPE question on Theorem MB that I deliberately refused to answer for it — Repair AE1's N4 gloss is "an assertion, not a supplied weakening proof". The held-out key is RE-KEYED to `ν = 12` after the measurement showed `ν = 11` was burned worse than by its four outings — its own answers are printed in this draft. GUARD E is built, is BIDIRECTIONAL, catches a dropped hypothesis a forward-only check would pass, and found a provenance defect in Theorem SL that the judge holding the file did not. And three of my own checkers defected on their first run — one of them VOIDED an admissible report** (2026-08-23, 04:3x–05:1x CDT)

**Slice.** One judge round harvested under the pre-registered gate order, one execution
claim **re-run here** rather than read, one held-out key retired and rebuilt at a new
range, one new guard built and demonstrated against three deliberate corruptions,
**zero** promotions, **zero** addresses minted, **zero** mathematics touched, and
**three defects in my own checkers** caught and recorded — one of which manufactured a
VOID verdict out of a parse error.

*(Clock note, once, as in §7.40 and §7.41: `date` on this box reads behind the ledger's
CDT convention. Times follow the ledger.)*

### (a) **Q39 — void gate blind and FIRST, then the execution claim, then the substance**

`problems/wowii/w61_r27_q39_gate.py` → `.out`. Graded against
`w61_r17_heldout_key.txt` (md5 `2c524fd4…`), with **r25's transcription guard re-run
first**: every key value the gate carries is re-read out of the key **file** before it
grades anything, `9/9` clean. A hand-copied key is an unchecked import.

| row | tier | key | Q39 | |
|---|---|---|---|---|
| **H1** | HAND | `14, 12, 16, 10` | identical, all four | **EXACT** |
| **H2** | HAND | `98 384` | identical | **EXACT** |
| **H2 terms** | HAND | ten terms | identical, all ten | **EXACT** |
| **H3** | NON-HAND | `791` + 10-way split | identical, split and all | **EXACT** |
| **H4** | NON-HAND | `131` | identical | **EXACT** |
| **H5** | HAND | `NO`, `16` | identical | **EXACT** |

**`7/7` EXACT. VOID GATE PASS — the report is admissible.** This is the **third** judge
ever to compute both non-hand rows on this line.

**And the execution claim was RE-RUN HERE, not read.** RULING CD: *a printed execution
artifact is not evidence; only re-running the code here counts.* The report's first line
declares `EXECUTION ENVIRONMENT: I ran code. Python 3.9 with NetworkX 3.2.1.` — a claim
that is **testable precisely because it also wrote the script to disk**. Running
`w61_S3_GFAN_r26_check.py` here reproduces **`68` of `68`** of the load-bearing lines of
its claimed `.out`, ending `SCRIPT_STATUS=PASS`
(`problems/wowii/w61_r27_q39_rerun.out`). **The execution claim STANDS**, on the same
footing as Gemini's and on a different footing from NVIDIA's printed `Output:` block.

### (b) **The narrow question is ANSWERED YES. The GAP MOVED.**

**Sent for one thing: is the Theorem K interface now supplied?** The IMPORT INTERFACE
ROLL CALL walks all eighteen names and returns **`18/18` statements present**, closing
with *"Nothing on the eighteen-name list is still unsupplied."* **The sole ground of the
r25 GAP is gone**, and the seven statements added last round hold up under an outside
reading. The **bracket count check I added** returns what it was built to return:
*"the bracket's count 'FOUR' is correct in both directions"* — the r24 under-count is
repaired and mechanically confirmed by a second party.

**Verdict is still `GAP`, and the ground is new:**

* **D1 (MATHEMATICS) — and this is the round's real return.** Repair AE1's **N4**
  sentence asserts the RIG chain consumes only `τ ≥ 2`; sol: *"MB1 is literally stated
  only 'in the hard core' … N4 asserts that the chain only consumes `τ ≥ 2`, but the MB
  proof is not supplied … **N4 is an assertion, not a supplied weakening proof.**"*
  **This is the J-SCOPE question the r26 brief printed and deliberately did not answer
  for the judge** — *"whether MB's proof consumes the rider is a J-SCOPE question and it
  is deliberately not answered for you here."* **The answer came back, and it did not
  come from me.** §7.41 said *"the gloss is now checkable, which is not the same as
  right — and if it is wrong, this is the round that finds out."* It is the round that
  found out. Refusing to restate Theorem MB weaker to make my own gloss true is what
  made this answer possible; had I tidied the scope note, the brief would have contained
  the AB2/AE1 defect and sol would have had nothing to check against.
  **Note what it does NOT do**: sol states plainly that the target corollary is itself
  filed in the hard core, *"so this gap does not furnish a counterexample to its
  implication."* Nothing is re-opened. Nothing is withdrawn.
* **D2, D3 (MATHEMATICS, both bracket-completeness).** The AZ bracket omits **RIG's
  universal-low guard** and **Theorem GFANν's residue-equality guard** from a roll call
  that claims to be complete. Both guards are *actually supplied* by the corollary, so
  the proof is sound and file-alone checkable; **the bracket's completeness claim is
  false.** This is RULING AZ's own species landing on the second AZ bracket this line
  has ever shipped — the r24 bracket under-counted, this one counts right and
  under-clauses. **The count is not the claim.**
* **D4, D5 (BOOKKEEPING).** RIG-1 over-cites Proposition L2(a); and — worth its own
  line — **D5 is RULING CD arriving from the judge's side**: *"The pasted diff
  establishes agreement of outputs, but it cannot establish that the programs were
  independently written."* A judge told us, unprompted, that our printed artifact is not
  evidence of the property we claimed from it. That is the same sentence we adopted this
  round, said back to us about our own file.

**Q39 buys ZERO, exactly as labelled in `qwen_queue.md` before the dispatch line was
sent. Corollary GFANν-HC stays at `1` family.** Its own prior engagement disqualifies
it, and a `GAP` return could not have promoted anything in any case.

### (c) **The re-key — and the burn was worse than "four outings"**

`problems/wowii/w61_r27_rekey.py` → `.out` → **`w61_r27_heldout_key.txt`, `ν = 12`.**

The task book said re-key because the r17 key has four outings. **The builder measured
the burn instead of assuming it, and the measurement is the finding:**

| the r17 key's own ANSWER values | hits in `notes/proofs/wowii61_draft.md` |
|---|---|
| `98 384` (H2) | **`8`** |
| `791` (H3) | **`10`** |
| `131` (H4) | **`5`** |
| `1 002` (H4 population) | **`7`** |

**`ν = 11` was not burned by outing count. Its answers are printed in our own ledger
thirty times over** — put there, correctly and openly, by the §7.35/§7.37/§7.39 harvest
tables that recorded each judge's row-by-row agreement. **A fifth outing on that key
would have been graded against numbers a reader of our own draft can read off.** The
"four outings" concern was real and the actual state was worse; this is the difference
between a shelf-life argument and a measurement.

**The new key, `ν = 12` (`2ν = 24`), same discipline as r17 and one addition:**
self-calibration against the printed `ν ≤ 10` draft data **before** any held-out value
(all rows PASS, and **RULING CD's rider is printed next to the PASS, not after it** — *a
passed calibration is not a passed harness; it says the generator understood the setup*);
OPS-3 met (`H1/H2/H5` HAND, `H3/H4` NON-HAND); `H3` is a survivor count over a **six-figure** shape
population with an 11-way per-`E` split; `H4` is a **four-figure** number of separate
Havel–Hakimi runs before the first digit. **Neither population size is printed here, and
that sentence is a repair**: the first draft of this very section printed both, and the
larger of them *is* `H2`'s answer — §7.42 (c) would have burned the key it was announcing,
in the same paragraph that records `ν = 11` dying that way. Caught by re-running the
leak probe against the draft **after** the section was appended. **The probe is now part
of the write, not part of the build.** **FAN-6′ misses over the whole `ν = 12` survivor roster:
`0`** — a fresh independent control on the theorem, obtained free with the key.
**The addition: a leak probe with a printed population** — `32` entries, every
discriminating value and every `H1`/`H5` shape counted against the draft **and against
all `34` shipped briefs**. `0` discriminating hits. **Burned range now recorded as
`ν ≤ 11`; the live key is `ν = 12`.**

〔**And the leak probe leaked.** On its first run it printed every `ν = 12` key value in
cleartext into a repo `.out` file **in order to prove that no key value appears in a repo
file.** That is the checker committing the exact species it hunts — the fourth instance
of RULING BL on this line and the second in two rounds — and it is *how `ν = 11`'s
answers reached the draft in the first place, one step earlier in the same pipeline.*
**Fixed at the root**: the probe now prints a digit-mask and the hit counts, which is all
a verdict needs. Verified by grepping the regenerated `.out` for all thirteen `ν = 12`
values: `0` hits.〕

### (d) **GUARD E — the gap I named before a judge did, and it is BIDIRECTIONAL**

`problems/wowii/w61_r27_guardE.py` → `.out`. §7.41 (g) said it in these words: *"GUARD B
proves a statement block EXISTS. It does not prove the statement is correct, or that it
matches the draft. Seven statements were transcribed into A.1 by hand and no guard checks
them against §7.5/§7.6/§7.2."* GUARD E is that check, and it is **mechanical**: it never
reads for meaning, it matches **math atoms** between the shipped brief and the draft.

**It runs in two directions, and the second is the one that matters.**

| direction | what it catches |
|---|---|
| **FORWARD** brief → source | an **ALTERED or INVENTED** clause |
| **REVERSE** source → brief | a **DROPPED HYPOTHESIS** — the AB2/AE1 species, the one that has actually cost this line rounds |

**A forward-only check passes a transcription that silently deletes `τ ≥ 2`.** That is
demonstrated below, not argued.

**Normalisation is DECLARED, not hidden.** The draft is Unicode and **§7.2 is in
Chinese**; the brief is ASCII. A `39`-entry transform table is required to compare them at
all, it is fixed before the run and **printed before any grading**, and it is applied to
**both** sides. Under RULING CE the forbidden move is adding a transform to silence a
specific mismatch — so the table never grew during the run, and every surviving mismatch
is adjudicated **by name with a reason**.

**Result on the brief as shipped to Q39: `39` atoms matched in block, `10` matched
ELSEWHERE in the draft (each with the draft line number, printed), `3` unmatched.**

**The one that is a real finding — and a judge holding the same file did not report it:**

〔**Theorem SL's (LOW3) form is attributed to a statement block that does not contain
it.** The brief prints, as *"Its (LOW3) form, which is the form cited below"*,
`Σ_{b∈B_lo} deg_A(b) + m̄ ≤ 2L + ν(B_lo) − 1`. GUARD E finds that inequality in the draft
at **line `1522` — inside Corollary MB1's PROOF**, not inside Theorem SL's statement,
which instead prints a *different* equivalent form (`… + e(B_lo) + m̄ ≤ L(L+3)/2 − 1`)
that the brief **omits**. Both directions fire on the same root. **The mathematics is
right** — §7.21 records the `2L + ν(B_lo) − 1` form as SL's, and Q39 itself grades this
import *"Match"*. **The provenance is wrong**: a judge asked to interface-check the
(LOW3) citation cannot verify it against the statement the brief supplies it under.
**That is precisely the residual half of the r25 GAP, one layer in**, and it is the kind
of thing GUARD B was structurally incapable of seeing.〕

The other two unmatched atoms are **one root**: §7.2 B writes `deg(v)` in its second
clause and `deg v` in its third, inside a three-line lemma, and A.1's transcription
regularised it. **Named, printed, reason given, and NOT normalised away** — widening the
transform table by one paren is exactly the move RULING CE forbids. It is carried as an
open item for the next brief build.

**Liveness — the guard shown FAILING, three times, on purpose.** RULING CD again: *a
printed PASS from a guard nobody has seen fail is the same evidence as a printed
`Output:` block.*

| corruption | species | GUARD E |
|---|---|---|
| `≤ L + ν(B_lo) − 1` → `+ 1` in Theorem MB | altered constant (the AZ species) | **FIRES**, both directions |
| **delete** `and τ ≥ 2` from Theorem SL | **dropped hypothesis** (AB2/AE1) | **FIRES** — and *only* the REVERSE direction sees it |
| `deg(b) ≥ τ + 1` → `≥ τ` in Theorem K | weakened rider | **FIRES** |

**And GUARD E states its own limits in its own output**, so a judge does not have to
find them: it matches **atoms, not meaning** (two clauses can share every atom and differ
in quantifier order); it cannot see a hypothesis the source itself leaves standing
outside its block (§7.2's standing reductio is a paragraph *above* Lemma S — which is why
it surfaces as MATCH-ELSEWHERE at line `559`, correctly located); and **it says nothing
about whether the SOURCE is right.** GUARD B said *the brief no longer lies about what it
contains*; **GUARD E says the brief's transcription matches the draft; neither says the
mathematics is true.**

### (e) **Three of my own checkers defected on their first run, and one of them VOIDED an admissible report**

RULING BL, and the population print is again what caught all three.

〔**The harvest gate manufactured a VOID.** Its H2-terms parser read the *whole* report
cell for integers and swallowed the `1` and the `10` out of the label
*"Terms for `E=1..10`"*, so ten terms that are **all correct** graded `*** WRONG ***`,
one HAND row failed, and the gate printed **`VOID`** on a report that is `7/7` exact.
**A checker that manufactures a defect is the single most dangerous failure mode on this
line**, and this one would have thrown away a round that answered the question it was
sent for. Direction of error: **against the judge** — the safer direction, and not an
excuse. Root fix: the term list is a plus-joined run, so parse *that structure*, not
"every integer in the cell".〕

The other two, both in GUARD E, both caught before any verdict was trusted:
**(E-2)** the block terminator treated a **blank quote line** (`>` alone) as the end of a
statement, and Theorem SL's statement contains two of them — so SL was truncated to two
lines and **all three of its displayed formulas** were reported as "matched elsewhere".
**(E-3/E-4)** the atom extractor admitted whole **prose lines** as math atoms whenever the
prose contained an `=`, and captured a bolded display **together with its sentence
period**, so one inequality read as both MATCHED and DROPPED. **Both erred against
ourselves**, like §7.40's comma defect and §7.41's `\b` defect. **Direction of error is
the thing to record.** Both root fixes are **CLASS** rules with printed populations — *a
run carrying three or more purely-alphabetic tokens is prose*, *sentence punctuation is
not part of a math atom* — and neither names a single word, because RULING S is that a
word list is not a class.

### (f) **What did NOT happen**

**No promotion.** Corollary GFANν-HC stays at **`1` family (Q35, Meta)**; Q39 bought zero
and was labelled as buying zero before it ran. **No address minted** — `R-24` remains the
lowest free row. **No mathematics touched**: §7.13 D untouched for the third round
running, no bounded weakening altered, D1 explicitly furnishes no counterexample and
nothing is re-opened. **No brief rebuilt** — GUARD E's findings and D1–D3 are the input
to the *next* build, not to a same-round patch. **The `ν = 11` key is retired, not
deleted**: it stays on disk as the graded record of Q36, Q37, Q38 and the NVIDIA
qualification. **The codex TUI lease is RELEASED.**

### (g) **Named next round**

1. **Rebuild the brief against D1–D3 and GUARD E.** Four repairs, all of them ours:
   **(i)** N4 — either restrict Theorem RIG's final bound back to the hard core, or
   supply the weakened-MB proof; **an assertion in a scope note is the AE1 defect and a
   judge has now named it as one.** **(ii)/(iii)** the AZ bracket gains RIG's
   universal-low guard and GFANν's residue-equality guard — *count right, clause
   complete*. **(iv)** Theorem SL's transcription carries the form its own statement
   block prints, or the brief says where the (LOW3) form actually lives.
   **Then re-run GUARD E on the rebuilt brief, and GUARD B, and the standing check that
   the brief supplies what any new guard cites.**
2. **The family bench is still empty and this round did not touch it.** The single
   highest-leverage unblock remains the `OR_KEY` `limit: 0` — with the user, correctly,
   since it is their money and their account setting.
3. **The live key is `ν = 12`.** Any qualification or family round from here grades
   against `w61_r27_heldout_key.txt`. **Burned: `ν ≤ 11`.**
4. **Unchanged and still queued:** the §7.25 own-attack family (Lemma C1-A, C1-A′,
   Proposition C1-B, Corollary C1-C, Theorem C1-2) at **0 rounds**; Conjecture C1 for
   `k ≥ 3`; the configuration/tier import sweep (§7.38 (f)) as a pre-dispatch gate.

**And the limit that rides with this round's headline, stated before a judge states it.**
GUARD E proves that the brief's transcription **matches the draft**. It does **not** prove
the draft is right, and it does **not** prove the *meaning* survived — it matches atoms.
Ten atoms this round are content that is genuinely in the draft but **not inside the block
the brief attributes it to**, and every one of those is a place where a judge doing an
interface check is sent to a statement that does not carry the clause. **`GUARD E` closes
the gap between *"the brief no longer lies about what it contains"* and *"the brief's
imports are transcribed faithfully"*. It does not close the gap to *"the brief's imports
are right"* — nothing mechanical does.**

### (h) **Round accounting**

| | |
|---|---|
| Q39 harvested | `problems/wowii/w61_S3_GFAN_r26.md`, verdict **`GAP`**, `19 432` bytes, 14m24s |
| void gate | **`7/7` EXACT, PASS** — third judge on this line ever to compute both non-hand rows; transcription guard `9/9` |
| execution claim | **RE-RUN HERE (RULING CD): `68/68` lines reproduced, `SCRIPT_STATUS=PASS` — STANDS** |
| the narrow question | **ANSWERED YES — `18/18` imports supplied, bracket count `FOUR` correct both directions; the r25 GAP's sole ground is GONE** |
| new GAP ground | **D1** N4 scope is an assertion, not a proof — **the J-SCOPE question the brief refused to answer, answered by the judge**; **D2/D3** bracket claims a complete guard roll call and omits two |
| re-key | **`ν = 12`**, `w61_r27_heldout_key.txt`; calibration PASS, leak probe `32` entries / `0` hits, FAN-6′ misses `0` over the whole roster |
| burn measured | `ν = 11`'s own answers appear **`30` times in our own draft** — burned far beyond its four outings |
| GUARD E | built, **bidirectional**, `39` matched / `10` provenance / `3` unmatched; **all three deliberate corruptions detected**, incl. one only the reverse direction sees |
| own-checker defects | **4** — a leak probe that leaked, a block terminator, a prose-admitting atom extractor, and **one that manufactured a VOID** on an admissible report; **a fifth caught at write time**: §7.42 (c) itself printed the new key's `H2` answer into the draft. All caught by printing the population or by re-running the probe |
| promotions | **none.** Corollary GFANν-HC stays at `1` family; `R-24` still the lowest free row |
| repairs to mathematics | **none.** §7.13 D untouched; no bounded weakening altered; nothing re-opened |
| lease | codex TUI **RELEASED** |
