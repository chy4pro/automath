## §7.39 owner-w61 round 24: **Q38 DISPATCHED to codex `gpt-5.6-sol` — the family-2 round on Corollary GFANν-HC, and the FIRST brief this project has written with RULING AZ known. The four AD1/AE1 sentences RIDE the round as a target joint, quoted verbatim. Two verdict-steering status leaks were caught by the build's own assert and removed — one of them had been in every brief since r14. The RULING BB mint-time guard is now a script, and its self-test immediately found a stale address in §7.38's own opening line** (2026-08-23, 01:5x–02:2x CDT)

**Slice.** One brief built (`prompts/w61_S3_GFAN_r24.md`, `108 368` chars, 27 anchored
patches off the r20 base), three pre-dispatch gates run and passed, one dispatch made and
hardcopy-verified Working, one address-uniqueness guard implemented rather than promised,
and one stale address fixed that the guard found on its first run.

### (a) **What this round is, and what it is the only thing between**

Theorem GFANν is PROVED-S3 (row R-23, §7.38 (a)). **Corollary GFANν-HC is at 1 family**,
and it is now the only statement standing between WOWII-61 and a closed S3 surface.
`cert_w61_r23.md` RULING BC authorises codex `gpt-5.6-sol` for one round, the family
options being genuinely exhausted: Meta holds family 1, Qwen is spent on this text, and
**Gemini is spent too** — *a re-read after repair is a re-read*, the rule that disqualified
Qwen from re-scoring repaired GFANν text, applied to Gemini unchanged.

**sol's strength, stated honestly rather than favourably** — this is a BC condition and it
is the ledger's job, not the planner's. sol is **not naive about this text**:

| | |
|---|---|
| Q33 (`w61_S3_GFAN_sol.md`, 43 KB) | five import GAPs; J-CAP PARTIAL on bookkeeping; both CAP bounds re-derived independently |
| Q34 (`w61_S3_GFAN_r14.md`, 57 KB) | **read Theorem GFANν and found its import defect** — D1, the Corollary MB1 mismatch, UPHELD, repaired as AC1. Also D2 (a briefing defect of ours) and D3 (Corollary RIG-1's "(d) directly" over-claim, AC2). Machine-verified the `E ≥ 1` roster in both directions |
| **what it has never done** | **judged Corollary GFANν-HC.** It has never been asked for a verdict on the corollary, and it has never seen the r18/r20/r24 text — not Section 0, not the rosters at `ν = 7…10`, not any repaired site |

The per-statement bar therefore counts this as family 2 for the corollary. **A reader must
be able to see both halves of that**: the family is legitimate under the bar, *and* sol
arrives carrying prior knowledge of the surrounding text and of one defect in it. Both are
written here so that neither has to be inferred from the other.

**Context isolation was still enforced**: `/new` before dispatch, hardcopy-verified (fresh
session banner, `gpt-5.6-sol high`, empty composer). sol's Q34 transcript is not in the
window; what it carries into this round is whatever survives in the model, not in the
session.

### (b) **RULING AZ applied at build time — the first brief written with it known**

RULING AZ: *ten of the eleven clear import edges in this document supply their guards
off-site, which makes them **unreviewable by any judge**, and that is the complete
explanation of the seven-round blind spot.* The response adopted is (c): **supply guards at
the import site even where off-site supply is legitimate.**

**On-site, in the target's own proof.** Corollary GFANν-HC's proof now carries a bracket,
immediately below it, naming every guard each of its three imports needs and where that
guard comes from: Theorem RIG's frame (present a fortiori from *in the hard core*) and its
`B_lo ≠ ∅` (the AD1 step above); Observation R1's `diam = 4` (frame); Theorem GFANν's
configuration, range and `L ≥ ν+1` (RIG, the case, and the line above). And it states the
**negative** too — **`τ ≥ 4` is available and is NOT consumed** — so that an
over-hypothesis reading is visible rather than inferred. The bracket closes by telling the
judge that every clause of it is an affirmative claim and that a guard claimed-and-unneeded,
needed-and-unclaimed, or claimed-from-a-place-that-does-not-supply-it are three distinct
MATHEMATICS defects. **A guard supplied at the import site is a guard the judge can score;
that is the entire point and it costs three sentences.**

**Off-site, and SAID SO — the part of AZ the task book asked for by name.** Three
obligations cannot move to an import site, and the brief now carries a block naming them:

1. **The `Fan(τ,L)` → `GFan(τ,L,ν)` configuration licence.** It is a section-preamble
   sentence governing a whole family of lemmas at once (*"Lemma FAN-1 applies word for
   word"*); attaching it per-import would assert it many times over. Declared **an
   assumption of this brief**, with the judge told that a `Fan`-stated lemma failing to
   survive the widening is a MATHEMATICS defect.
2. **The canonical meaning of "the hard core"** (frame + reductio + `τ ≥ 4`), fixed once in
   the vocabulary rather than at ~15 statements. **Repair AE1 is what happens when a
   statement quietly uses a different meaning**, and the brief says so and invites a search
   for others.
3. **Roster completeness**, withdrawn with its evidence printed inline.

That block is the honest half of AZ: an obligation carried elsewhere is still an obligation,
and a judge that does not know it exists cannot report that it could not check it.

### (c) **The AD1 + AE1 sentences RIDE the round — quoted, not assumed**

Four sentences across four sites, written by us, reviewed by nobody. **Assuming them clean
is exactly the move that produced AE1** — that repair was the same species and the same
statement as an earlier one three lines away. They are landed into the brief verbatim from
the repaired draft and then handed to the judge as **TARGET joint J-NEWTEXT**, quoted in
full: **(N1)** Corollary MB1, **(N2)/(N3)** the same sentence at Corollary GFAN2-HC and at
the target itself, **(N4)** the AE1 licensed reading inside Theorem RIG's statement.

The joint asks three things per sentence, not one: is it **true**, is it **needed** (a true
sentence supplying a guard nobody required is its own defect class here), and does it
actually **supply what it claims to supply**. (N2)/(N3) being one sentence at two different
sites is called out, with the invitation to find that the sites are not interchangeable.
And **(N4) is flagged as what it is** — an affirmative claim about an import's hypotheses,
made in a scope note, which is precisely the shape J-SCOPE exists to distrust.

**The cost, stated rather than hidden.** Marking these four sites tells the judge where to
look and gives up some of the round's blindness. That is a real price and it was paid
deliberately: unmarked new text is read as settled text, and this line has now twice shipped
a repair whose own sentence nobody checked. The brief does **not** say what the sentences
were written in response to, and tells the judge not to guess.

### (d) **Two verdict-steering status leaks, caught by the build's own assert**

The build asserts that **no favourable status word for Theorem GFANν survives into the
product** — `PROVED-S3`, `separately certified`, `two independent families`,
`already certified`. It fired twice, and both hits were **inherited, not new**:

> *"Do not referee the imports' own proofs — **they are separately certified**."* (J-IMPORT)
> *"**Each of these is separately certified.** Your job with them is import matching only."*
> (Appendix A.1 preamble)

Under RULING AZ these are worse than untidy: **an import is an interface, and telling the
judge the interior is settled answers J-IMPORT for it.** The second has been in every brief
of this family since r14 — including the two that scored the Part-1 promotions. Both are
replaced with wording that says the imports' proofs are not printed and not this round's
question, that their **statements** are, and that **which of them have been reviewed
elsewhere is deliberately not disclosed**. Recorded as a *briefing* defect of ours, not a
mathematics one.

This is also RULING Q's pre-dispatch adversarial brief pass doing its job on its second
outing: on its first it caught a verdict-steering rubric of mine, and on this one it caught
a verdict-steering *status word* that four previous briefs shipped.

### (e) **Three pre-dispatch gates, all passed, with the calibration failure reported**

`problems/wowii/w61_r24_gate.py` → `.out`.

| gate | what it is | result |
|---|---|---|
| **1 — `brieflint`** | round 22's artifact, **used as a build gate for the first time**. The rules are not re-typed: the gate execs `w61_r22_brieflint.py`'s definitions and calls its own `lint()`, because a second copy of a rule is a rule that can drift | `0` findings on r24; **`4` on the r20 base**, so the gate can still fail |
| **2 — RULING AX derivability** | per-row, not per-substring: *could a judge produce this value without doing the work the row tests?* | all rows clear |
| **3 — B6 by role-of-object** (RULING AU) | everything the target must **use** is disclosed in full; everything held out lives at `ν = 11` / partitions of `22`, which have **no mathematical role** in the target — its range is `ν ≤ 10` and its conclusion is `ν ≥ 11` | disclosure `5/5`, boundary stated to the judge |

**H2 is asserted DELIBERATELY derivable and the gate says so out loud.** Its closed form is
printed on purpose; that is what makes it a *fabrication* discriminator rather than a
capacity test, and if the formula were ever removed H2 would silently become a HAND row no
judge can reach. A check that passes for the wrong reason is a check that will pass when it
matters.

**The calibration failure, reported rather than only the pass (RULING BA).** Gate 2 v1 ran
**unscoped** and returned four LEAKs — **all four of them its own Section 0 question text**,
which necessarily names `ν = 11`, prints H5's shape and writes H4's definition. It also read
(C-4)'s heading *"the count is a closed form, and every survivor is printed"* as a closed
form for the **survivor** count, when it is one for the **shape** count `S(ν)` in a sentence
that mentions both. Both failures are **noisy-and-safe**, not the silent-and-favourable
direction — but the second is the *same conflation the row exists to test*, so it is written
down and the scoping (`BODY` = brief minus Section 0) is documented in the script rather than
applied quietly.

**Tiers pre-registered AND disclosed** (RULING A, `cert_677_r15`): `H1/H2/H5` **HAND** — a
stated wrong value **voids** the round; `H3/H4` **COMPUTATIONAL** — `CANNOT COMPUTE` costs
nothing, a wrong value triggers a **traced** downgrade of the claims that actually rest on
it, and **all** computational rows confidently wrong with the hatch unused **voids**. r20
disclosed the boundary but not the penalties; the penalties are now in the frozen brief,
before the answer.

### (f) **The r20 structural withdrawal — this round is its TEST, and that is said BEFORE the harvest**

Round 20 made the roster-completeness withdrawal **structural**: the control figures and the
round-17 diff artifact are printed inline, a binding rubric line forbids a `GAP` whose *sole*
ground is the explicitly withdrawn obligation, and the withdrawal's own evidence is declared
a live target. **No round has yet probed it.** Q37 returned a GAP on the corollary that was
**not** on completeness, so the mechanism was never exercised.

**This round is its test, and the possible outcomes are named now, blind:**

1. sol attacks the withdrawal's **evidence** and reports it as a defect — the withdrawal
   worked exactly as designed;
2. sol returns a `GAP` grounded solely on completeness anyway — the rubric line does not
   bind, and the withdrawal is not structural but decorative;
3. sol neither attacks nor invokes it — **untested again**, and it must be recorded as still
   untested rather than as passed.

Outcome 3 is the one a favourable reading would quietly convert into a pass, which is why it
is written down before the report exists. **Stated BEFORE the harvest and not after.**

### (g) **RULING BB implemented — and it found a stale address on its first run**

`problems/wowii/w61_mint.py` → `.out`. Two lines have now had independent address
collisions, and **both were found by an assertion that happened to run at grep time.**
The guard is now a shared module with `mint(family, n)` (refuses a spent address) and
`next_free(family)` (the caller does not pick a number at all). It lives in one file
because a check re-typed per script is a check that drifts — which is precisely how the
r23 `R-22` collision survived.

**Self-test on the live draft:** `R`-family addresses spent `23`; lowest free **`R-24`**;
minting the spent `R-23` raises. **No address was minted this round** — no repair landed
and no registry row moved — so the guard's first real job was its own listing, and that
listing found:

〔**BB1** — `§7.38`'s **opening Slice line still read *"One promotion executed (row
R-22)"***, three lines above the numbering note that renumbered it to R-23 and explained
why. The row is R-23 everywhere else, including the registry table and the `§7.13 D` status
line. Fixed here to `R-23`. **This is the fifth appearance on this line of *"the change
landed but the record of it did not"*** — and note what caught it: not the r23 closing grep,
which asserted *"R-22 present exactly once"* and was satisfied by §7.31's legitimate R-22,
but a **listing of the whole address space printed before any verdict** — RULING AS's
lesson, arriving in the address layer.〕

### (h) **Dispatch record**

| | |
|---|---|
| round | **Q38** |
| target | **Corollary GFANν-HC** (`ν ≥ 11`, `L ≥ 12`), §7.13 D — one statement, family 2 |
| judge | codex `gpt-5.6-sol high`, screen session `codex`, `/new`-isolated |
| brief | `prompts/w61_S3_GFAN_r24.md`, `108 368` chars, md5 `45b79f35f9cd42dd29ddecb31dfacdde` |
| builder | `problems/wowii/w61_r24_build_q38.py` — 27 anchored patches off `w61_S3_GFAN_r20.md` (md5 `6fc6cc06…`, `97 617` chars), `+10 751` chars |
| gates | `w61_r24_gate.py` → `.out` — brieflint `0/4-on-base`, AX derivability clear, B6 disclosure `5/5` |
| key | `problems/wowii/w61_r17_heldout_key.txt`, **not pasted**; `ν = 11` |
| expected outputs | `problems/wowii/w61_S3_GFAN_r24.md`, `_check.py`, `_check.out` |
| dispatch | one short line into the TUI pointing at the brief; hardcopy full-screen confirms **`Working`** |
| harvest | next round |

**Gate pre-stated blind, before any report exists.** The void gate is `§7.32 (h)1` as
amended by the tier table above: `H1`/`H2`/`H5` wrong ⟹ **VOID**; `H3`/`H4` `CANNOT
COMPUTE` ⟹ clean; `H3`/`H4` wrong ⟹ traced downgrade; **both** computational rows
confidently wrong with the hatch unused ⟹ **VOID**. Grading is exact-match against the key
file, which was written in round 17 and has not been touched since.

**Where the eleven-clear sweep verdict is cited, its limits ride with it** (§7.38 (f)):
that verdict's guard vocabulary is **numeric/non-emptiness only** — `L ≥ n`, `ν ≥ n`,
`τ ≥ n`, `B_lo ≠ ∅`. **Symbolic** (`L ≥ ν+1`), **configuration** (`Fan` vs `GFan`) and
**tier** (frame / reductio / hard core) guard classes **remain unswept**, and the §7.13
preamble licence quoted in the brief's off-site block **was found by hand, not by the
tool.** The AZ statistic *"ten of eleven supplied off-site"* is a statement about the swept
population and nothing wider.

### (i) **Named next round**

1. **Harvest Q38.** Void gate first, blind, before any prose is read; then the r20
   structural-withdrawal outcome recorded as 1, 2 or **3**, with 3 recorded as *still
   untested* rather than quietly as a pass.
2. **J-NEWTEXT and J-AZGUARD are the two rows to read first** in the returned joint table.
   A `CLEAN` on J-AZGUARD is the first evidence this project will have that a locally
   supplied guard is actually locally checkable — the claim RULING AZ rests on.
3. **Extend the import sweep to configuration and tier guards** (§7.38 (f)) and run it as a
   pre-dispatch gate the way brieflint now is.
4. **Apply RULING AZ to the remaining nine off-site edges** in the draft itself, not only in
   briefs. The brief is downstream; the draft is where the defect class lives.
5. **Unchanged and still queued:** the §7.25 own-attack family (Lemma C1-A, C1-A′,
   Proposition C1-B, Corollary C1-C, Theorem C1-2) at **0 rounds**; then Conjecture C1 for
   `k ≥ 3`.
