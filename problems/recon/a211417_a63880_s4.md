# S4 独立二轮查重：A211417 一般 r 猜想 + A63880 powerful core

日期 2026-08-17（第二轮，独立于我方 13:00 前后的一轮）。方法：WebSearch + arXiv API 直查（`export.arxiv.org/api/query`，摘要精确匹配 "A211417"/"A063880"/"Bala"+"divisibility"）+ OEIS 页面实时 curl 抓取（非缓存，页面 footer 显示 `Last modified August 17 14:26 EDT 2026`，即今天）+ `google-deepmind/alphaproof-nexus-results` 与 `google-deepmind/formal-conjectures`（auto_oeis 分支）GitHub raw 内容直读。

---

## 目标1：A211417 / Bala D(r) 整除猜想（一般 r）

### Verdict：**OPEN 确认**（一般 r 情形无任何新证明；r=1 情形已证，与我方已知一致）

### 证据

1. **OEIS A211417 页面（今日实时抓取）**：
   - COMMENTS 区仍保留原始猜想措辞："More generally, for r >= 1, we conjecture that there exists a constant D(r) such that D(r)*a(n)/Product_{i = 1..r, i coprime to 30} (30*n - i) is integral for all n." —— 用词仍是 "we conjecture"，无任何"已证"标注。
   - Ralf Stephan 2026-06-30 评论逐字确认：AI agent 证明的是 "The conjecture that (30*n-1) divides a(n)"，即仅 r=1 情形，未提及一般 r。
   - LINKS 区只有一条 AlphaProof Nexus 链接，文件名为 `oeis_a211417_conjecture_specific.lean`（specific = r=1），无 general 版本链接。
   - 无 7/8 月新增评论。

2. **`google-deepmind/alphaproof-nexus-results` 仓库（GitHub raw 直读，非搜索引擎缓存）**：
   - `APNOutputs/OEIS/oeis_a211417_conjecture_specific.lean` 最终定理为
     ```
     theorem target_theorem_0 (n : ℕ) : (30 * (n : ℤ) - 1) ∣ (a n : ℤ)
     ```
     —— 仅 r=1，证明用 p-adic 赋值 + Legendre 公式，与 Ralf Stephan 评论描述一致。
   - `APNOutputs/OEIS/` 目录内**只有这一个 A211417 相关文件**，无 general 版本。
   - README 明确声明该仓库"only contains problems where successful proofs were discovered"——即一般 r 版本不在其中 = AlphaProof Nexus 未解出。
   - 最近 6 次 commit（2026-05-13 至 2026-06-05）均与 A211417 无关（Stacks Project、Erdős 列表等），无新增。

3. **`google-deepmind/formal-conjectures`（auto_oeis 分支，今日直读，比搜索引擎更权威——这是官方"已尝试但未必解出"的完整问题池）**：
   - 文件 `FormalConjectures/OEIS/Auto/211417_afcfaa38.lean` 中：
     ```
     theorem oeis_a211417_conjecture_general (r : ℕ) (hr : r ≥ 1) :
       ∃ D : ℕ, D ≥ 1 ∧ ... := by
       sorry
     ```
     **`sorry` 状态，今日仍未闭合** —— 这是官方仓库对一般 r 情形状态的最直接、最新鲜的证据，与我方"open"判断完全吻合。
   - 同文件中 `oeis_a211417_conjecture_specific` 定理体也是占位（该分支只存问题声明，真正证明存在于 alphaproof-nexus-results 独立仓库，二者对应关系符合 README 描述）。

4. **arXiv 精确检索**（`abs:"A211417"`、`abs:"Bala" AND abs:"divisibility"`、`abs:"factorial ratio"` 相关）：**零命中**。搜索引擎层面能找到的唯一相关 2026 年新论文是 arXiv 2607.18313《A Proof of Bala's Congruence Conjecture for A028342》——**不同序列**（A028342，关于排列/因子数的同余，非 A211417 的整除猜想），与本题无关，仅同为 Peter Bala 猜想家族、易混淆，需注意甄别。
   - 另确认 AlphaProof Nexus 主论文 arXiv 2605.22763（"Advancing Mathematics Research with AI-Driven Formal Proof Search"）本身即只解出 r=1，与已知一致。

5. **MathOverflow / Zulip / X**：定向搜索未发现任何讨论 A211417 一般 r 情形或 D(r) 猜想的帖子/推文。

### 结论
四条独立信源（OEIS 页面实时状态、alphaproof-nexus-results 仓库文件内容、formal-conjectures 仓库 `sorry` 占位、arXiv 精确摘要检索零命中）一致指向：**一般 r 的 Bala D(r) 整除猜想截至 2026-08-17 仍是 OPEN**，无人（含 AlphaProof Nexus）解出。我方声称的证明若成立，将是首个解决。**建议：不要因为找不到反证就掉以轻心——请再核实我方证明本身逻辑严密性，因为该问题官方仓库明确标注为已尝试未解（sorry），说明至少 AlphaProof Nexus 曾经攻过但失败，存在实际难度，需格外小心假证明陷阱。**

---

## 目标2：A63880

### Verdict：**OPEN 确认**（无变化）

### 证据

1. **OEIS A063880 页面（今日实时抓取）**：COMMENTS 区最新一条仍是 Amiram Eldar，2024-08-31，内容：
   - primitive terms（真因子均不在本序列中的项）均为 powerful numbers（A001694）；
   - 其余项形如 m*s（m 为 primitive/powerful，s 为与 m 互素的 squarefree 数）；
   - "The only primitive term below 10^18 is 108."
   - "If there are no other primitive terms, then a(n) = 108 * A276378(n)." —— 条件句，说明"是否还有其他 primitive term"仍未解决，这正是"powerful core"缺口所在。
   - 无 2025/2026 新评论，无新链接。

2. **arXiv 精确检索**（`abs:"A063880"` / `abs:"A63880"`）：**零命中**。通用搜索（"powerful core" OEIS、"sigma(n) mod 216" 2026）也未发现任何 2026 年相关论文或解决声明。

### 结论
A63880 的 powerful core 缺口（"10^18 以下只有一个 primitive term 108，是否穷尽"）**截至今日仍是 OPEN**，无外部进展，可继续攻。

---

## 双目标汇总

| 目标 | Verdict | 关键新证据 |
|---|---|---|
| A211417 一般 r（Bala D(r)） | **OPEN 确认** | formal-conjectures `211417_afcfaa38.lean` 中 general 定理仍为 `sorry`（今日直读）；alphaproof-nexus-results 仅有 r=1 证明文件；OEIS 页面/评论无变化；arXiv 摘要零命中 |
| A63880 powerful core | **OPEN 确认** | OEIS 页面自 2024-08-31 起无更新；arXiv 摘要零命中 |

两题均可放心继续推进，无第三方抢先。目标1 因官方仓库存在 `sorry`（说明曾被 AI 系统尝试但未解），建议对我方证明做加倍严格的独立复核。
