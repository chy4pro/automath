# TICKET K6-LEAN8 — assemble the stratum-(b) theorem in RANK FORM, unconditionally, from LEAN6 + LEAN7

Light ticket (Lean only; do not start any scan or msolve — the machine is loaded). Continue in
`lean/proofenv`; all K1695 modules must stay green. Add ONE file `K1695/StratumBAssembly.lean`
(import `K1695.StratumB` and `K1695.RankCriterion`). Deliver: file, `lake env lean` output with
`#print axioms` (no axiom beyond [propext, Classical.choice, Quot.sound]), `lake build` green,
`engine/harvest/k1695_r6_lean8/REPORT.md` ending with `DONE-K6LEAN8`. No `sorry` in anything listed
as proved. Budget ~2 h.

Available (read their exact statements in the source first):
- `stratumB_root_shift_rank_eq_two` (RankCriterion.lean): for A over F with `minpoly F A = m`, m
  irreducible of natDegree 2, and μ : L a root of m (L any field extension), `(A.map (algebraMap F L)
  − μ • 1).rank = 2` — no separability / non-base-field hypothesis.
- `stratumB_rank_at_root_of_simple_extension_and_nullity` (StratumB.lean): under its hypotheses (L
  generated by an integral quadratic μ, μ a root of m, and `hnull`), `(A_L * Matrix.swap L a b − μ • 1).rank = 3`.
- `l4_t0` (TranspositionLemma.lean): if `(A − μ•1).rank = n` then `(A * P_τ − μ•1).rank ≥ n − 1`.

**A1 (root case, unconditional).** Discharge `hnull` with `stratumB_root_shift_rank_eq_two`:
`theorem stratumB_rank_at_root (…) : (A.map (algebraMap F L) * Matrix.swap L a b − μ • 1).rank = 3`
for every root μ of m in L (keep whatever hypothesis on L the LEAN6 theorem needs about L being
generated by μ; if it needs `L = F⟮μ⟯`-style generation, state it; try to remove it if the proof
does not really use it).
**A2 (nonroot case).** For μ : L with `aeval μ m ≠ 0` (μ not a root of m): `A.map … − μ • 1` is
invertible — proof: m(A) = 0 and the polynomial identity m(X) − m(μ) = (X − μ)·q(X) (Mathlib:
`Polynomial.X_sub_C_dvd_sub_C_eval` / `Polynomial.mul_divByMonic_eq_iff_isRoot`, or write q explicitly
for a monic quadratic: m = X² + tX + s, so m(X) − m(μ) = (X − μ)(X + μ + t)); evaluating at A_L gives
(A_L − μ)(A_L + μ + t) = m(A_L) − m(μ)·1 = −m(μ)·1, invertible. Hence `(A_L − μ•1).rank = 4` and by
`l4_t0`, `(A_L * swap − μ•1).rank ≥ 3`.
**A3 (assembly).** `theorem stratumB_rank_form (A : Matrix (Fin 4) (Fin 4) F) (hA : IsUnit A.det)
(m) (hm : Irreducible m) (hdeg : m.natDegree = 2) (hmin : minpoly F A = m) (a b : Fin 4) (hab : a ≠ b)
{L} [Field L] [Algebra F L] (μ : L) : (A.map (algebraMap F L) * Matrix.swap L a b − μ • 1).rank ≥ 3`
— for EVERY field extension L and every μ ∈ L (root or not). If A1's L-generation hypothesis cannot
be removed, state A3 for L = the field generated by μ, and separately for an algebraically closed L
via `IntermediateField.adjoin`… — or explain exactly what remains. Also note in the report that the
bridge "∀ μ in an algebraic closure, rank ≥ n − 1 ⟹ minpoly = charpoly" (R1) is still open, so A3 is
the rank form of the stratum-(b) theorem, not yet its minpoly form.
Add an `example` instantiating A3 at a concrete F = ZMod 3, m = X² + 1, A = C_m ⊕ C_m.

## Report
Theorem names, PROVED / NOT PROVED, axioms, build log. End with `DONE-K6LEAN8`.
