You are leading a research campaign. Assume a complete proof of the target exists and find it; work as an aggressive multi-agent team with an explicit route table and adversarial audit; do not search the internet; do not use earlier conversations. Partial progress is not the deliverable; return only when a complete proof has survived your own adversarial audit; if nothing survives, return only the strongest rigorously proved derivation as numbered lemmas, its exact remaining gap as a precise statement, and every machine-checkable object you built. Write every proof directly in your reply (files in your sandbox are invisible to me); print any verifier script verbatim in a fenced code block.

# Target: pin the exact threshold of the fractional hinge inequality (Erdős–Surányi function g(n))

Setting. Atom system: finitely many prime powers p^j (j ≥ 1) with weights α_{p,j} ≥ 0 and Σ_j α_{p,j} ≤ 1 for every prime p; S(n) = Σ_{p,j} α_{p,j}[p^j | n]. For m ≥ 1 and a window I = {x+1,…,x+m},
   (H_c)   Σ_{k=1}^{m} (S(k) − c)^+ ≤ Σ_{b∈I} (S(b) − 1)^+ .
Two facts are proved (both refereed; the first also kernel-checked in Lean 4):
  (i) (H_c) holds for c = 56501/6400 = 8.8283 (and for c = 97/10, formalised).
  (ii) (H_c) FAILS for every c < 2: take r primes p_1 < … < p_r in an interval [M, √2·M) with M > 2, all weights 1, m = p_{r−1}p_r, and the window of length m centred at P = p_1⋯p_r. Then in [1,m] each of the C(r,2) pair products occurs exactly once and no integer carries three of the primes, so the left side is C(r,2)(2 − c); in the window only P carries two of them, with S(P) = r, so the right side is r − 1. Hence (H_c) fails once c < 2 − 2/r, and such clusters exist for every r because Σ 1/p diverges.
Transfer (kernel-checked): (H_c) for all atom systems with real c ≥ 1 implies g(n) ≤ ⌈cn⌉ + 2n. So c = 2 would give g(n) ≤ 4n; c = 4 gives 6n; c = 7 gives 9n. Erdős conjectured g(n) ≤ 2n; that is out of reach of this inequality, so 4n is the floor of the whole route and c = 2 is exactly the boundary case.

## Targets, equal rank
T1: prove (H_2). By (ii) this is sharp — the infimum threshold would be exactly 2, and g(n) ≤ 4n follows.
T2: refute (H_c) for some c with 2 ≤ c < 56501/6400: an explicit atom system, m and window with exact arithmetic (we re-check). Any such c raises the proved floor of the whole route and is as valuable as T1.
T3 (fallback, only after T1 and T2 freeze): prove (H_c) for the smallest c you can, in the order 7, 6, 5, 4, 3 (each unit is worth n in g(n) ≤ ⌈cn⌉ + 2n).

## The proved architecture at c = 8.83 and exactly where it stops
Constants Q = 2^16, H* = 1025/1024, ρ = 9/8, T = 401/75, D_0 = 3601/1280, c = D_0 + ρT.
L1 moments and optimised hinges: Σ_{n≤N} e_r((f_p(n))_p) ≤ N H(f)^r/r! for f_p ≤ 1; for a ≥ r−1, v = ⌊ra/(r−1)⌋, A_r(a) = (v−a)/C(v,r) gives (Σu_i − a)^+ ≤ A_r(a) e_r(u) on [0,1]^N; and z^{Σu_i} ≤ ∏(1 + (z−1)u_i) for the exponential moments.
L2 large atoms and the dense branch. S_0 keeps the atoms p^j ≤ m/Q. If an omitted atom q > m/Q divides k ≤ m then k/q < Q < 510510 = 2·3·5·7·11·13·17, so ω(k) ≤ 7 and S(k) ≤ 7; hence (S(k) − c)^+ = (S_0(k) − c)^+ ONLY FOR c ≥ 7. Dense branch (H(S_0) ≥ H*): V = (H*/H)S_0, Σ_{k≤m} min(V,7) ≥ m((1 − 1/Q)H* − H*^8/8!) > m, so Σ min(S_0, c) ≥ m and the affine certificate S_0 − 1 closes it — again using c ≥ 7.
L3–L4 rounding and carriers. Levels 𝒟 = {1} ∪ {8,…,15}/2^h (ratio 9/8); retention q_p(t) ≤ m^{η(t)} with η(t) = 16t/27 for t ≤ 1/8 and a table η(v/64) = E_v/720 above (E_9 = 60, …, E_64 = 240) chosen so that every shortest prefix of mass > 1 has Σ η ≤ 2/3, hence P_C ≤ m^{2/3} and every negative modulus P_C q ≤ m; S_0 ≤ ρB + D_0 with B = Σ b_p; L_B = Σ_{k≤m}(B − T)^+; carriers = shortest prefix of mass > 1, θ_C the last level, ν_C = ⌊m/P_C⌋, c_C = M_C/ν_C. Outside a carrier the levels are ≤ θ_C, the outside mass exceeds T − 1 − θ_C, so the cofactor k/P_C has ≥ 4, 5, 6 prime factors (by the discreteness of 𝒟) and ν_C ≥ 210, 2310, 30030.
L5–L9 certificate. c_C ≤ min_r θ A_r((T − s_C)/θ)(H*/θ)^r/r!; F(n) = Σ_C λ_{θ_C} c_C [P_C | n](1 − U_C(n)/K_{θ_C}) with U_C = Σ_{p∉C} min(b_p, θ_C), λ_θ = ρK_θ/(K_θ − H* − δ(θ)θ) and δ from the prime densities π(ν)/ν ≤ 9/40, 3/20, 1/8; the window value uses N_I(P_C) ≥ ν_C and N_I(P_C q) ≤ ν_C/q + 1 with the +1 errors charged as θ_C π(ν_C); pointwise feasibility via the counts D_{j,L,d}(N) = [X^{L+d}](G^N − (G − X^j)^N); 57 finite scales < 990415/10^6 and an analytic tail < 1/1000.
THE OBSTACLE BELOW 7 (this is the crux of T1 and T3): the truncation S → S_0 and the dense branch both need c ≥ 7, because a point k ≤ m divisible by a large atom can carry S(k) up to 7 while S_0(k) is much smaller. To go below 7 you must handle the large-atom points on their own: each omitted atom q > m/Q has fewer than Q multiples in [1,m], and every such point has at most 7 distinct prime factors; bound Σ over those points of (S(k) − c)^+ by a separate certificate (candidates: charge them to the window using the two-sided counts #{b ∈ I : d | b} ≥ ⌊m/d⌋ for the same moduli; or lower Q and pay in the dense branch; or split by ω(k) and use a hinge of order ω), and redo the dense branch with Σ min(S_0, c) ≥ m at the smaller c (which needs a higher moment order via A_r(a) with a = c, and a mean threshold adapted to c).
WHAT THE BARRIER (ii) TELLS YOU ABOUT THE EXTREMAL SHAPE: the tight instances are r primes of nearly equal size with every pair product just below m and the window centred at their product — all the mass of the left side sits on points with S = 2 and the right side has a single point with S = r. Any proof of (H_2) must be tight on this family (both sides vanish at c = 2 there, so it is the boundary case), and any refutation for c ≥ 2 must beat it. Test every candidate certificate on this family FIRST, at several r, in exact arithmetic.

## Dead routes (do not re-derive): nonnegative certificates (refuted on reflected windows (m!−m, m!]); two-sided counts alone (projective planes); Bernoulli rounding / threshold averaging; clean reductions refuted on Hensley–Richards windows (an interval can contain more integers free of small primes than [1,m] does — test every intermediate statement there and on CRT dense windows at 10^4–10^6 before using it).

## Routes (route table with advantage / weakness / obstacle / verification bridge; no route more than a quarter of the effort before a judge decision; the only progress metric is the gap sentence "what is missing for ALL atom systems, all m, all windows"; two unchanged gap sentences ⇒ freeze the route and lower the target): R1 a separate certificate for the large-atom points, unlocking c < 7; R2 the dense branch at small mean with high moment order; R3 a signed certificate designed to be tight on the prime-cluster family of (ii) (the boundary case), aiming directly at c = 2; R4 exact refutation search for 2 ≤ c < 8.83 on CRT-adapted, reflected and prime-cluster windows (exhaustive over small prime systems and all window residues). An adversarial agent stress-tests every candidate on the prime-cluster family, reflected windows, CRT dense windows and Hensley–Richards windows BEFORE any proof effort.

## Output contract: every lemma PROVED / CONDITIONAL (on what) / REFUTED / OPEN; complete proofs written in the reply; the certificate as an explicit finite family with its pointwise feasibility proof and value computation; exact-arithmetic verification of every numerical constant, script printed verbatim; final block "final claim ← lemmas ← unproved items"; the best threshold c fully proved, or the largest c for which you refute (H_c), and the resulting bound g(n) ≤ ⌈cn⌉ + 2n.
