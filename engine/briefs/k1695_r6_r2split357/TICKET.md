# K6-R2SPLIT-357 — extend the certified case decomposition of the rank-two stratum to characteristics 3, 5, 7 (screen: sol; ONE msolve at a time under run_capped 2.5 GB; box must have swap < 10 GB)
Context: K6-R2SPLIT closed characteristic 2 (engine/harvest/k1695_r6_r2split/REPORT.md; the line re-checked all 6 U-charts × 128 assignments independently: registry §R6.93–§R6.94) — hence Kourovka 16.95 holds for n = 4 over every field of characteristic 2. Your encoder r2split_encoder.py already implements the case inventories for p = 3, 5, 7 (transpositions/double transpositions: E_1, E_{-1}, N; 3-cycles: E_1, E_ω, E_{ω²} (p = 5, 7 — ω adjoined by ω² + ω + 1 = 0, or ω ∈ GF(7) explicit), E_1 only for p = 3 (ω = 1); 4-cycles: E_1, E_{-1}, E_{±i}, N — with i adjoined by i² + 1 = 0 where GF(p) lacks it). Pilot nodes ran; the trees are OPEN.
Task: for each p ∈ {3, 5, 7} (in this order), run the DPLL search to closure with the SAME rules (swap gate, one msolve, run_capped --wall 600 --mem 2500000, resumable state), over all six U-charts or the three symmetry representatives (state which, and prove the symmetry argument as in the char-2 report). Which permutations to branch on: in odd characteristic all six transpositions CAN be simultaneously bad on the rank-two stratum (registry §R6.42 N4n: a GF(7) Klein-four family; §R6.85: GF(3) matrices with all 4-cycles bad and others with all 3-cycles bad), so the char-2 selection (six transpositions + (12)(34)) will NOT close — branch on a larger set: start with the 9 permutations of types (2,1,1) and (2,2), then add the 3-cycles and 4-cycles as needed; a branch closes when its ideal is unit; a branch that stays non-unit after ALL 23 permutations have cases assigned is a candidate all-bad configuration: solve it (msolve -P 1), test the point with cyclic() over the extension field, and report it fully (disclosure: do not interpret; it would be a counterexample to 16.95). Controls per characteristic: the realised case pattern of a concrete rank-two matrix over GF(p) (e.g. for p = 3 the matrix rows 0211/0010/0100/1120 of registry §R6.85, all 3- and 4-cycles bad) must be NON-unit; the "all six transpositions E_1" conjunction is expected NON-unit in odd characteristic (§R6.42) — record what it gives. Report: per-characteristic branch trees, run counts, max branch time/memory, closed/open status, the list of permutations that sufficed; DONE-r2split357. A closed characteristic p = 16.95 at n = 4 over every field of characteristic p (subject to the line's independent re-run, as for p = 2). Never publish outward; nothing is SOLVED.
