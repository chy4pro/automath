# CODEX TICKET (Lean 4; sol tier) — the (E') REDUCTION LEMMAS (sub-magma and quotient
# lifting) and a `decide` upgrade of the switching base facts.
# Repo: $HOME/workspace/claudecode/automath; project lean/etp677_ext (conventions of
# Ext677Window.lean: X6U/X6W/L13*/L14X6 predicates; window_lift_X6 already proves the QUOTIENT
# lifting). 0 sorry; own `#print axioms` for every new theorem appended to AXIOMS.txt.
# Heavy processes ≤ 2 (lake build). DONE marker: DONE-LEANERED.

## Background
(E') := "some a ∈ Im(U) satisfies X_6" (L14X6) — the surviving existence target of the fibre-3
programme. Banked (Lean, window_lift_X6): if φ : M ↠ B is a surjective magma hom of finite E677
magmas and X6 holds at U_B(x) in B, then X6 holds at U_M(x̃) in M for any lift x̃.
NEW, to formalise (all elementary):
R1 (sub-magma lifting). Let N ⊆ M be a sub-magma (closed under op) of a finite E677 magma M;
   N with the restricted op is E677 (and E255 if M is), and for x ∈ N every unary term
   W, U, P, F, S and every window term (v, c, d, b, …) computed in N equals the one computed
   in M (left division in M of elements of N lands in N because L_x restricted to N is a
   bijection of the finite set N). Hence: X6 at a in N ⟹ X6 at a in M, and a ∈ Im(U_N) ⟹
   a ∈ Im(U_M). Formalise N as a `Subtype` with a closure hypothesis, or as a predicate
   `IsSubmagma op (S : Set M)`.
R2 (monogenic reduction of (E')). Corollary of R1 with N = the sub-magma generated by one
   element g (define as the least closed set containing g, or as the closure of {g} under op
   via an inductive predicate): if every finite MONOGENIC idempotent-free E677+E255 magma
   satisfies (E'), then every finite idempotent-free E677+E255 magma does. (Idempotent-
   freeness passes to sub-magmas trivially.)
R3 (combined reduction, statement only if R1/R2 done): (E') for M follows from (E') for any
   sub-magma or any quotient of M that is idempotent-free E677+E255 — so a minimal
   counterexample to (E') is "simple and generated by each of its elements" in the sense
   that no proper idempotent-free sub-magma and no idempotent-free proper quotient with an
   Im(U)-X6 point exists. State cleanly; proof = R1 + window_lift_X6.
D  `decide` upgrade: replace `native_decide` by `decide` (or `fin_cases`/`omega`-style
   exhaustive tactics, or `by decide` after `revert`) in the eight base facts of
   Ext677Switch.lean (switch_isolation, rotating_switch_isolation, switch_base_E677_eval,
   rotating_switch_base_E677_eval, switch_base_E255_eval, rotating_switch_base_E255_eval,
   switch_base_idempotent_iff, rotating_switch_base_idempotent_iff) — 49 or 7 cases over
   ZMod 7 each; keep the theorem names; re-run #print axioms and update AXIOMS.txt so that
   switch_E677 / switch_E255 / switch_idempotent_iff and the rotating versions become
   clean-tier. If kernel `decide` times out on ZMod 7 arithmetic, use `Fin 7` with explicit
   `decide` on a Bool-valued check plus a bridging lemma.

## Deliverables
lean/etp677_ext/Ext677EprimeRed.lean (R1–R3), the edited Ext677Switch.lean (D), AXIOMS.txt
updated, engine/out/codex/etp677_lean_eprime_red_report.md ending with DONE-LEANERED.
