Follow-up (round 4, same rules). Lemmas 1–5 of round 3 are verified (I checked the flow structure, the chain argument, and both pair counts). So f(n) = 3 for 6 ≤ n ≤ 11 and the smallest n with f(n) ≥ 4 lies in [12, 19] (the round-1 construction has 19 lines through 3 layers of 6 points). Now r = 12. By enumeration the shapes (s₁,s₂,s₃) with s₁s₂ ≥ 12, s₂s₃ ≥ 12, s₁+s₂+s₃ ≤ 11 are: (2,6,2), (2,6,3), (2,7,2), (3,4,3), (3,4,4), (3,5,3), (3,6,2), (4,3,4), (4,4,3).
General tools you may use: (i) every modulus a has ALL its window multiples inside the covered set S = S₁ ∪ S₂ ∪ S₃, forming an arithmetic progression of difference a with at least one term per block; a "loop" is a 3-term progression x < y < z (one per layer) and distinct moduli give distinct differences; (ii) loops at y ∈ S₂ correspond to distinct pairs (x,z) ∈ S₁×S₃ with x + z = 2y; (iii) the boundary maps are injections into S₁×S₂ and S₂×S₃; unused pairs create the imbalance δ, non-loop edges go upward and form chains from β-points to α-points (generalise round 3's Lemma 2 to several unused pairs per side).
T1: for each of the nine shapes, either prove it impossible for a Hall-deficient set of size 12 (giving f(12) = 3), or exhibit an explicit 3-layer system (sets S₁, S₂, S₃ of integers and 12 progressions with distinct differences, each progression's terms all inside the layers, one or more terms per layer as the block structure requires) with |S₁|+|S₂|+|S₃| ≤ 11 — I will CRT-embed and verify it. Shapes with s₁s₂ = 12 or s₂s₃ = 12 have a bijective boundary map on that side. T2: if all nine are impossible, state what r = 13 needs. Every claim with a status tag; explicit constructions only; end with "final claim ← lemmas ← unproved items".
