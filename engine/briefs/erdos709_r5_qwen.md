Follow-up (round 5, same rules). Lemmas 1–5 and 7 of round 4 are verified. Now close the six open r = 12 shapes or find a construction. Tools to combine: (i) Lemma 2's flow facts (top point loops = d⁺(top), bottom point loops = d⁻(bottom), cumulative δ ≤ 0, non-loop edges strictly upward with each non-loop edge a having v_a < w_a both multiples of a in the middle block, so w_a − v_a ≥ a, while u_a = v_a − a ∈ S₁ and z_a = w_a + a ∈ S₃); (ii) loops at y are distinct pairs (x,z) ∈ S₁×S₃ with x + z = 2y, and the two extreme sums min+min, max+max have multiplicity 1 in S₁ + S₃; more generally the multiplicity of a sum s in S₁ + S₃ is at most min(|S₁|, |S₃|) and the number of sums with multiplicity ≥ k is limited (count pairs: Σ_s mult(s) = |S₁||S₃|); (iii) the total loop count is r − (#non-loop edges) and every non-loop edge consumes one out-slot at its tail and one in-slot at its head; (iv) distinctness of moduli: loops at the same y have distinct x (hence distinct a = y − x); a non-loop edge from y to y′ has a | (y′ − y) and a = y − u for some u ∈ S₁, so a ≤ y′ − y. For each shape (2,6,3), (3,6,2), (3,4,4), (4,4,3), (3,5,3), (2,7,2): enumerate the possible numbers of non-loop edges and the loop-count vectors forced by the degree constraints, then test against the pair budget |S₁||S₃| minus unusable extreme pairs, and against distinctness. Either prove all six impossible (giving f(12) = 3) or exhibit an explicit system (layers as integer sets, all twelve progressions listed with their differences) — I will verify by computation. Status tags on every claim; end with "final claim ← lemmas ← unproved items".
