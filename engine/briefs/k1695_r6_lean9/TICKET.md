# K6-LEAN9 — close R1 in Lean: "rank(B − μ·1) ≥ n − 1 for every μ in an algebraic closure ⟹ minpoly B = charpoly B", then assemble the kernel-checked stratum-(b) theorem for n = 4 (screen: sol; ONE lake process, ≈6 GB — run only when no other lake build or msolve is running; 0 msolve)
Context: lean/proofenv/K1695/ contains RankCriterion.lean, CyclicToMinpoly.lean, StratumB.lean, StratumBAssembly.lean (registry §R6.80: `stratumB_rank_form` — for A ∈ GL(4,F) with minpoly A = m irreducible quadratic, every transposition swap and every scalar μ in every extension L satisfy 3 ≤ rank(A.map ι * Matrix.swap L a b − μ•1)). What is missing (R1): the passage from the rank form to the minimal-polynomial form. Mathematical proof (notebook, registry §R6.29): a matrix B over a field K is cyclic (minpoly B = charpoly B) iff for every eigenvalue μ in the algebraic closure K̄ the geometric multiplicity is 1, i.e. rank(B − μ·1) ≥ n − 1; the direction we need: if for every μ ∈ K̄ rank(B − μ) ≥ n − 1 then minpoly = charpoly. Proof sketch: over K̄, B is similar to a Jordan form; rank(B − μ) ≥ n − 1 means one Jordan block per eigenvalue, so the minimal polynomial over K̄ has degree n; minpoly and charpoly are invariant under field extension (Mathlib: `minpoly.algebraMap_eq`-type lemmas for matrices / `Matrix.minpoly_map`? check `LinearMap.minpoly` and `Matrix.charpoly_map`), so equality holds over K. Alternative route avoiding Jordan forms: use the rational canonical form / `Module.End` cyclic decomposition in Mathlib, or the criterion "the K̄[X]-module K̄ⁿ is cyclic iff every eigenspace is 1-dimensional" (`Module.End.eigenspace`, `LinearMap.IsSemisimple`, `Polynomial.degree_minpoly`…). Any correct Lean proof is acceptable; keep to Fin 4 if the general n is harder.
Deliverables: (1) `lean/proofenv/K1695/RankToMinpoly.lean` with `theorem minpoly_eq_charpoly_of_rank_ge (B : Matrix (Fin n) (Fin n) K) (h : ∀ (L : Type) [Field L] [Algebra K L] (μ : L), n - 1 ≤ (B.map (algebraMap K L) - μ • 1).rank) : minpoly K B = B.charpoly` (or the Fin 4 version, or with L := AlgebraicClosure K only — state clearly); (2) `StratumBMinpoly.lean`: `theorem stratumB_minpoly (A) (hA : IsUnit A.det) (m) (hm : Irreducible m) (hdeg : m.natDegree = 2) (hmin : minpoly F A = m) (a b : Fin 4) (hab : a ≠ b) : minpoly F (A * Matrix.swap F a b) = (A * Matrix.swap F a b).charpoly` derived from stratumB_rank_form + (1) — this is Kourovka 16.95 for stratum (b) at n = 4, kernel-checked; (3) `lake env lean` outputs and `#print axioms` for both theorems (only propext, Classical.choice, Quot.sound); register the files in lakefile.toml if the project lists modules. No sorry/admit/native_decide. Report to engine/harvest/k1695_r6_lean9/REPORT.md and create DONE-lean9. Never publish outward; nothing is SOLVED.
