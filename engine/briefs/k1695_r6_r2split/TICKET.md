# K6-R2SPLIT — 16.95 at n = 4 on the rank-two stratum by CASE DECOMPOSITION into small certified sub-ideals (screen: sol; ONE msolve at a time under run_capped 2.5 GB; start only when the box has swap < 10 GB)
Why: the monolithic rank-two ideals (40 and 36 variables, "all 23 permutations bad" with a free eigenvalue variable each) exceed 54 GB in degree 6–7 (registry §R6.83, §R6.89). The rank-ONE stratum was closed by certificates on SMALL systems (§R6.75: one ideal per symmetry orbit of a subset, each solved in seconds). Do the same for rank two: split "σ is bad" into finitely many polynomial CASES and certify the conjunction case by case.
Setting: A = I + UWᵀ, U, W ∈ F^{4×2}, det A = det(I₂ + WᵀU) ≠ 0 (variable z with cz − 1); WLOG (chart, §R6.79) W = [[1,0],[0,1],[w₃₁,w₃₂],[w₄₁,w₄₂]] — 12 variables (u₁₁…u₄₂, w₃₁, w₃₂, w₄₁, w₄₂). AP_σ = P_σ + U W_σᵀ, (W_σ)_{j,·} = W_{σ(j),·}. Badness of σ ⟺ ∃t: rank(P_σ − tI + U W_σᵀ) ≤ 2. CASE SPLIT for each σ (proved in engine/harvest/k1695_r6_r2hand/PROOF.md and k1695_r6_r2char2/PROOF.md — read both): (E) t an eigenvalue of P_σ (t ∈ {1} ∪ roots of unity of order dividing a cycle length: 2, 3, 4 — over the prime field these are explicit, e.g. t = −1 for even cycles, t = ω for 3-cycles) — badness ⟺ rank K_c ≤ 4 − g (bordered matrix, all coranks), a polynomial condition in U, W WITHOUT t; (N) t not an eigenvalue — badness ⟺ I₂ + W_σᵀ(P_σ − tI)⁻¹U = 0 (Woodbury), i.e. four polynomial equations in U, W, t with (P_σ − tI)⁻¹ = adj/det explicit (for a 4-cycle: (P³ + tP² + t²P + t³)/(1 − t⁴)); keep t as a variable with the extra generator (1 − t^{ord})·z_σ − 1 forcing t not a root of unity of the relevant order (or simply χ_{P_σ}(t)·z_σ − 1).
Task: 1. For characteristic p = 2 first (then 3, 5, 7): build, for every one of the 23 non-identity σ, the list of its cases (E_{σ,t} for each eigenvalue t of P_σ in F̄_p — note in characteristic 2 the eigenvalues are only 1 (2- and 4-cycles) and 1, ω, ω² (3-cycles; ω ∈ F₄ — handle by adjoining ω with ω² + ω + 1 = 0 as a variable), and N_σ). 2. The theorem "no A with all 23 bad" is the statement that for every choice of one case per σ, the ideal generated by the union of the chosen case-conditions plus cz − 1 (plus the ω relation) is the UNIT ideal. The number of choices is large (∏ (#cases_σ)), so organise it: (a) first certify the SMALL conjunctions that already give the unit ideal — e.g. "all six transpositions bad at t = 1" alone (registry §R6.87: over GF(2) exact rank two never has all transpositions bad, so this small ideal is likely unit in char 2 — test it), "all three double transpositions bad" plus a few others, "the six 4-cycles bad in case N" etc.; (b) build a DPLL-like search: pick σ₁ with the fewest cases, branch, add generators, run msolve (seconds each, 2.5 GB cap), prune any branch whose ideal is already unit, continue; record each closed branch as a certificate file; the theorem in characteristic p is proved when every branch is closed. Reuse the encoder pattern of problems/k1695/round6_gc4_cert.py (own script; controls: a branch known to be consistent — e.g. the bad set of a concrete rank-two A over GF(2) such as rows 0111/1011/0001/0010 with its actual (σ, t) cases — must come out NON-unit). 3. Report: the branch tree, per-branch msolve times and results, total branches closed, any OPEN branch (= a candidate all-bad configuration: try to solve it with msolve -P 1 and test the point with cyclic() over an extension; report fully, do not interpret), and the characteristic(s) completed. Characteristic 2 complete = 16.95 at n = 4 over every field of characteristic 2 (subject to the line's re-run); likewise 3, 5, 7.
Deliverables under engine/harvest/k1695_r6_r2split/: encoder + search script, certificates (.ms/.gb per closed branch), REPORT.md with statuses PROVED/COMPUTED/OPEN, DONE-r2split. Load rule: one msolve at a time, run_capped 2.5 GB, wall 600 s per branch (a capped branch is OPEN-due-to-load, not evidence). Never publish outward; nothing is SOLVED.
