# K6-AUDIT4 — adversarial audit of the complete n = 4 chain before publication (screen: sol; light Python; ≤ 1 msolve under run_capped 2.5 GB for spot re-runs; Lean only if the box is quiet: ≈6 GB, one lake process)
Audit target (registry problems/k1695/campaign_registry.md §R6.66, §R6.93–§R6.103): "For every field F of characteristic 0, 2, 3, 5, 7, 11, 13, 17, 19 and every A ∈ GL(4,F) there is a permutation matrix P with AP cyclic." Chain: (1) normalisation: if all 24 AP_σ are non-cyclic then A has an eigenvalue λ ≠ 0 of geometric multiplicity ≥ 2 in an extension, so λ⁻¹A = I + R with rank R ≤ 2 (and scaling commutes with the family); (2) rank R ≤ 1: certificates (engine/harvest/k1695_r6_n4x/, problems/k1695/round6_rank1_certify.py, round6_r2_ideal.py rank-one control); (3) rank R = 2: W-chart (W = [I₂; w] by conjugation + GL₂ gauge) and six U-charts cover the exact-rank-two locus; per permutation "bad ⟺ ∨_r E_r ∨ N" over the eigenvalue set of P_σ in the algebraic closure; the DPLL trees (engine/harvest/k1695_r6_r2split/, r2split357/, r2splitq/ and the line's engine/harvest/k1695_r6_r2split_check/, k1695_r6_r2split_odd/) with every terminal a unit ideal over GF(p) or ℚ; (4) Nullstellensatz + extension invariance turn "unit over the prime field" into "no point over any field of that characteristic".
Tasks: attack every link. (a) Is the normalisation right when the eigenvalue λ lies only in an extension? (b) Does the GL₂ gauge/conjugation really preserve "some AP_σ is cyclic"? Check the direction of conjugation on the permutation set. (c) Are the case lists exhaustive for EACH p (which roots of unity are eigenvalues of each cycle type; degenerate cases p = 2, 3; ω, i adjoined correctly as variables and never assumed in GF(p) when absent)? (d) Is N correctly "t not an eigenvalue" — does the Woodbury criterion require det(P_σ − tI) ≠ 0 exactly? (e) Re-derive the encoders' generator sets from the definitions for two random permutations and compare with the .ms files of BOTH families (line's and codex's) — any discrepancy is a finding. (f) Spot re-run 20 random unit terminals per characteristic with msolve and confirm [1]; check that DPLL children exhaust the parent's cases (no missing case). (g) Try to construct a counterexample directly: random search over rank-two A over GF(p^k), k ≤ 3, p ∈ {2,3,5} for an all-24-bad matrix (there must be none). (h) Lean parts (n ≤ 3, stratum (b)): rebuild only if the box is quiet; otherwise audit the statements' meaning. Report: AUDIT.md with every finding (severity, whether it breaks the theorem), DONE-audit4. Never publish outward; nothing is SOLVED.
