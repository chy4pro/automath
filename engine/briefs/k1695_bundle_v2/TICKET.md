# K6-BUNDLE-V2 — materialise the compact release asset recorded by K6-PUBREV (screen: codex10, which built it; no msolve/Singular/lake; nothing pushed or uploaded)
Problem: `engine/harvest/k1695_github_pkg/repo5/release/SHA256SUMS` records `bf5046f9b75e63e6281280d2d03f7b80dd4966e89820251d28d4c5de2695cb05  kourovka-16-95-certificates-v2.tar.zst` (25 795 157 B, 19 507 members per ACCEPT5.log F1), but NO such file exists anywhere under engine/harvest/ — the asset was hashed but not kept (or was removed). The GitHub release needs the actual file.
Task: (1) Rebuild the asset from the same member list `repo5/BUNDLE_INDEX.sha256` (path + SHA-256 per member; members come from the harvest directories used in K6-PUBREV — 1723 branch outputs, the line's odd/char-2 trees, p13 chart-5 audit, self-test inputs, evidence/control logs); verify EVERY member's SHA-256 against the index before packing (any mismatch or missing member → stop and report). (2) Pack deterministically: `tar --sort=name --mtime='2026-08-30 00:00Z' --owner=0 --group=0 --numeric-owner -cf - <members> | zstd -19 -T1 -o kourovka-16-95-certificates-v2.tar.zst` (bsdtar on macOS: use the equivalent `--options` for a fixed mtime/uid/gid, or a small Python `tarfile` writer with sorted names and zeroed mtime/uid/gid — state exactly which). (3) Write the file to `engine/harvest/k1695_github_pkg/bundle/kourovka-16-95-certificates-v2.tar.zst`, compute its SHA-256 and size; extract it into a temp dir and verify member count 19 507 and every member hash again. (4) If the SHA-256 equals bf5046f9… — done. If it differs (tar metadata), update BOTH `repo5/release/SHA256SUMS` and any other file that records the asset hash (grep repo5 for bf5046f9), and make ONE new commit on repo5 (author chy4pro <chy4pro@users.noreply.github.com>, message "Release asset v2: reproducible archive hash") — no history rewrite. (5) Report `engine/harvest/k1695_github_pkg/BUNDLE_V2.md`: file path, bytes, SHA-256, member count, the exact pack command, verification results, and whether a commit was made (hash). DONE-bundle_v2. Never publish outward.
