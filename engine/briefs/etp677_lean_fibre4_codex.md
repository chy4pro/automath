# CODEX TICKET (Lean 4; sol tier) — FIBRE-4 EXCLUSION (with the E255 defect) in Lean, labelled
# tier: Lemma 1 symbolic + one bv_decide finite lemma. Registry R46 STEP 63/64.
# Repo: $HOME/workspace/claudecode/automath; project lean/etp677_ext (reuse
# Ext677Core7Free.lean / Ext677Core7Hand.lean: Core7Pair, core7Chains, the semantic bridge
# core7_pattern_chains, the fibre transport and coordinatisation used by fibre3_exclusion; and
# Ext677Fibre2.lean's fibre_card_mul_left). 0 sorry; own #print axioms appended to AXIOMS.txt —
# the finite lemma may carry ONE bv_decide certificate axiom (name it); everything else must be
# standard. MEMORY RULE: set_option maxHeartbeats 400000 / maxRecDepth 4000; never `decide` over
# an enumeration of Fin 4 → Equiv.Perm (Fin 4); the finite lemma goes through bv_decide only.
# Heavy ≤ 2 (lake build). DONE marker: DONE-LEANF4.

## Statement
theorem fibre4_defect_exclusion {M B} [Finite M] [Finite B] (opM opB) (hM : E677 opM)
  (hB : E677 opB) (h255 : E255 opB) (φ : M → B) (hφ : Surjective φ) (hhom : IsMagmaHom opM opB φ)
  (m₀ : M) (hfail : ¬ E255At opM m₀) (ha : Nat.card {m // φ m = φ m₀} = 4) : False
Corollary (with Ext677Minimal): a minimal counterexample is simple or every non-trivial proper
congruence class has size ≥ 5 (`minimal_counterexample_no_class_le_four`).

## Proof to formalise (all verified by line-677; notation: T_{x,y}(s) = σ_{x,y}(s) ∈ Perm)
Let a := φ m₀, e := the fibre coordinate of m₀ (choose a bijection fibre(a) ≃ Fin 4 sending m₀
to 0 — this is the WLOG "e = 0"). Terms u = U a, w = W a, p = P a, b = F a, d = b*b, v = d*b,
c = W b, e0 = a*a (S a). Operations: X = T_{u,a}, C = T_{a,u}, D = T_{a,w}, A = T_{a,a},
Bop = T_{S(a),a}; the Core-7 operations as in Ext677Core7Hand (14 pairs).
L1 (Lemma 1, symbolic, any fibre size): E677(a,a) lifted: D_t(C_s(Bop_{A_t(s)}(t))) = s (chain
   through aa = S a, S(a)·a = u, a·u = w, a·w = a); E677(a,u) lifted: X_t(D_s(C_{X_t(s)}(t))) = s
   (chain through ua = a, au = w, aw = a). Put α = A_e(e), β = Bop_α(e). Then (i) D_e(C_e(β)) = e;
   (ii) the E255 failure at m₀ says X_β(e) ≠ e (the product ((m₀ m₀) m₀) m₀ has fibre
   coordinate X_β(e)); (iii) if X_t(e) = e for some t then D_e(C_e(t)) = e, hence t = β
   (D_e ∘ C_e injective), contradicting (ii). Conclusion NH: ∀ t, X_t(0) ≠ 0.
L2 (finite lemma, bv_decide): the Core-7 chains on Fin 4 (permutation-matrix encoding exactly as
   in Ext677Core7Free but with 4 coordinates: 14 pairs × 4 rows × 16 Booleans + chain aux) plus
   NH (¬ X_t(0) = 0 for the four rows t) is unsatisfiable. Reference CNF (kissat UNSAT + DRAT
   verified in minutes): problems/etp677/simple/fibre_core/out/p7nh_m4.cnf (generated by the
   snippet in registry STEP 64 addendum 2 from core_joint.py: `build(4, SETS['p7'])` + unit
   clauses ¬sig[(u,a)][t][0][0]). Build the Lean Bool formula the same way as Core7Free's
   core7Formula (multiplexer lookups over BitVec), prove `core7nh4_unsat : ∀ a, formula a = false`
   by bv_decide (expect a few minutes; if it exceeds 10 min, report timings and stop).
L3 Bridge: from a semantic family of Core-7 chains on Fin 4 satisfying NH build a satisfying
   assignment of the Bool formula (as core7Semantic_satisfies does for Fin 3) → contradiction.
L4 Transport: fibre sizes equal along left translations (fibre_card_mul_left / equal_fibres);
   the fibres over the eight Core-7 terms and over S(a), u have card 4; choose bijections with
   Fin 4 (m₀ ↦ 0 over a); define the T's; the lifted E677 chains hold (core7_pattern_chains +
   two new instance lemmas for E677(a,a) and E677(a,u) with the products aa = S a, S(a)·a = U a
   — X1_U_iterate / window lemmas), and the defect reads X_β(0) ≠ 0.
## Deliverables
lean/etp677_ext/Ext677Fibre4.lean, AXIOMS.txt appended (exactly which theorems carry the one
certificate axiom), engine/out/codex/etp677_lean_fibre4_report.md ending with DONE-LEANF4.
