# K6-PAPER5 — add the sparse-support and constancy theorems (every n, every field) to the paper (screen: any free codex; TinyTeX under $HOME; no solvers; nothing published — the line reviews and pushes nothing for this paper step)
Inputs: `engine/harvest/k1695_paper/main4.tex` (current paper, title already covers n ≤ 4 + the n = 5 rank-one stratum) + `refs3.bib` (unchanged), `engine/harvest/k1695_r6_r1alln_pro.md` (the proofs to transcribe, §1–§9), `problems/k1695/round6_r1alln_check.py` + its PASS output (registry §R6.123), registry §R6.123.
Task: produce `engine/harvest/k1695_paper/main5.tex` from main4.tex with ONE new section "Sparse and constant rank-one families in every dimension" containing, in the paper's own notation and with COMPLETE proofs (rewrite the harvest's Chinese proofs in English; do not cite the harvest):
- Setup: the deflation configuration c₀ = x, c_k = e_k + y_k x (m = n − 1) and the Krylov determinants D_{j,τ} (already in the paper's n = 5 section — reuse it).
- Lemma A (feedback invariance): det K(M + bℓᵀ, b) = det K(M, b); corollary D_{0,τ} = det[x, P_τx, …, P_τ^{m−1}x].
- Lemma B (cycle exclusion): an omission whose function graph has ≥ 2 cycles is identically bad (left 1-eigenvector + PBH), so only Hamilton paths and path+one-cycle omissions matter.
- Lemma C (dimension reduction): y_j = 0 reduces the m-dimensional configuration exactly to the (m−1)-dimensional one (proof by feedback + block determinant), hence any minimal counterexample has all y_i ≠ 0.
- Proposition D (m ≤ 2 by hand, all characteristics incl. 2): the four-candidate contradiction.
- Theorem E (sparse family): for every n and every field, |supp(y)| ≤ 2 ⟹ some D ≠ 0; pulled back: |supp(u)| ≤ 3 or |supp(w)| ≤ 3 ⟹ some AP_σ cyclic for A = λ(I + uwᵀ) (the transpose trick (AᵀP)ᵀ = PᵀA ∼ APᵀ for the u-version).
- Path identities (state and prove by direct expansion; they are verified computationally in the repository): χ_v + g_v X_v = t^m q_v; D_v ≠ 0 ⟺ gcd(χ_v, g_v) = 1; the path+fixed-point characteristic polynomial Φ = (t−1)χ_v − x_f y_f t^ℓ and the badness criterion g_v(α) = 0 ∧ (α−1)q_v(α) = x_f y_f.
- Theorem F (constancy family): x constant on supp(y) (up to one exceptional coordinate) ⟹ some D ≠ 0, with the two-case proof ((t−1)q_v = c g_v + t − λ; swapping arguments); pulled back to u/w as in the harvest §9 (9.2).
- One honest closing sentence: these families do not close the rank-one stratum for general (u, w); Conjecture J remains open.
Also: abstract + intro get ONE added sentence ("For every dimension, we further prove the rank-one cases in which u or w has support at most three, or is constant on the support of the other vector."); update the OPEN/outlook wording accordingly. NO other changes; refs3.bib unchanged; the hedge sentences stay.
Then compile (build5/, pdflatex+bibtex+pdflatex+pdflatex, zero undefined) → main5.pdf + BUILD5.md (page count, warnings, hashes, the rendered new-section title). Deliver DIFF5.md and DONE-paper5. The proofs must be mathematically complete and self-contained — if any step of the harvest proof does not convince you while writing, STOP at that step and report it in DIFF5.md instead of papering over it. Never publish outward; nothing is SOLVED.
