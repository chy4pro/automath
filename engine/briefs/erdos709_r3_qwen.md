Follow-up (round 3, same rules). Your Lemma 2 is verified (I confirmed by enumeration that (2,5,2) is the only shape for r = 10, and your f(6) ≥ 3 certificate with max A = 23 checks out by direct computation). Now r = 11 with three blocks. By enumeration the only integer shapes with s₁s₂ ≥ 11, s₂s₃ ≥ 11 and s₁+s₂+s₃ ≤ 10 are (2,6,2) and (3,4,3). The bijectivity trick fails (|S₁×S₂| = 12 > 11), so the degree identity Σ(w_a − v_a) = 0 is no longer automatic.
T1: prove that neither shape can occur for a Hall set of size 11 (so f(n) ≤ 3 for n ≤ 11, hence f(11) = 3), OR construct an explicit A with |A| = 11 and a window of 3·max(A) consecutive integers whose neighbourhood has ≤ 10 points (give A, the window start, and every multiple of every a in the window; I will verify by computation). Useful structure: for shape (2,6,2), the 11 edges v_a → w_a on S₂ have out-degrees ≤ 2 (sum 11) and in-degrees ≤ 2 (sum 11); exactly one point of S₁ × S₂ and one of S₂ × S₃ are unused. For (3,4,3): |S₁×S₂| = 12, one pair unused on each side. Work out the forced structure (which a have w_a = v_a, the "loop" edges y with y ± a in J₁, J₃, and the non-loop edges of positive length) and derive a contradiction or a construction. T2: if you find a construction, minimise max(A). Every claim with a status tag; every construction explicit; end with "final claim ← lemmas ← unproved items".
