# K6-LEAN10 — generalise the kernel-checked stratum-(b) theorem from n = 4 to every even n (screen: sol; ONE lake process ≈6 GB, only when no other lake build runs; 0 msolve)
Context: lean/proofenv/K1695/StratumBMinpoly.lean (kernel-checked, registry §R6.86): for A ∈ GL(4,F) with minpoly A = m irreducible of degree 2, every transposition swap gives minpoly(A·swap) = charpoly(A·swap). Hand theorem (registry §R6.3, proved in the notebook): for n = 2k and A ∈ GL(n,F) whose F[X]-module structure has invariant factors (m, m) with m irreducible of degree k (equivalently minpoly A = m irreducible of degree n/2 — check that this is equivalent for n = 2k: minpoly irreducible of degree k forces F^n to be a free F[X]/(m)-module of rank 2, hence invariant factors (m, m)), every transposition τ gives A·P_τ cyclic. Proof idea (as in the n = 4 Lean files): for μ a root of m in an extension L, A − μ has rank n − k... — re-derive: over L ⊇ F(μ), A is diagonalisable-like with each eigenvalue of m of geometric multiplicity 2; P_τ = I − ddᵀ (d = e_a − e_b) is a rank-one update; rank((A − μ)P_τ + μ ddᵀ)-type computation gives rank(AP_τ − μ) ≥ n − 1 at the roots of m, and at non-roots A − μ is invertible so a rank-one update keeps rank ≥ n − 1; then the R1 lemma (rank ≥ n − 1 at every scalar of an algebraic closure ⟹ minpoly = charpoly) — generalise K1695.RankToMinpoly from Fin 4 to Fin n.
Task: (1) generalise `minpoly_eq_charpoly_of_rank_ge_four` to `minpoly_eq_charpoly_of_rank_ge (n) (B : Matrix (Fin n) (Fin n) K) (h : ∀ L …, n − 1 ≤ rank(B.map − μ•1)) : minpoly K B = B.charpoly`; (2) generalise the stratum-(b) rank bound to Fin (2k) with m irreducible of natDegree k and minpoly A = m (state precisely; if the n = 4 proof relied on quadratic-specific identities — RankCriterion.lean's `quadratic_*` lemmas — replace them by the module-theoretic argument or by a direct rank computation for rank-one updates of A − μ with kernel of dimension 2 at each root); (3) assemble `stratumB_minpoly_general`. Deliverables: new file(s) under lean/proofenv/K1695/ registered in lakefile.toml, lake output, #print axioms (standard three only), no sorry/admit/native_decide; REPORT.md and DONE-lean10 under engine/harvest/k1695_r6_lean10/. If the general case resists, deliver n = 6 (m cubic) as a partial. Never publish outward; nothing is SOLVED.
