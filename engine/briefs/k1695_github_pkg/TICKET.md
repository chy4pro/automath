# K1695-GITHUB-PKG — the public verification/falsification repository for the n ≤ 4 results (screen: codex10/11 or luna; ENGINEERING ONLY — no solver runs except the verify.sh smoke test under run_capped 2.5 GB, one msolve; one Lean build ≈6 GB only when the box is quiet)
Owner directive (verbatim, 08-30 07:1x): "之前的成果做好了直接对外，GitHub要有哈，论文也要发，x也要发，要全面，如果有要发布的代码发GitHub上，要提供让其他人验证和证伪的全部条件". Publication happens ONLY after (i) "n = 4 over every field" is closed (registry §R6.104) and (ii) gates G1 audit / G2 literature / G3 certificates pass, and (iii) the owner's final go through dialogue — this ticket BUILDS the repository locally now so that publishing is a `git push` later. Git identity: chy4pro (name/email [email]) — NEVER the owner's private email, never engine transcripts, never machine paths or usernames (grep the whole tree for "user", "/Users/", "chenhaoyu" before DONE — zero hits required).

## Repository: `kourovka-16-95` (new, dedicated; to be created under github.com/chy4pro by dialogue at publication time; build it at engine/harvest/k1695_github_pkg/repo/ as a local git repo with the layout below; the paper source lives in chy4pro/automath-papers and links here)
```
README.md            — statement of what is proved, how, and how to verify/falsify in 5 minutes / 1 hour / 1 day
CLAIMS.md            — one line per claim: statement, method (Lean | certificates), status, which verify.sh target checks it
REPRODUCE.md         — exact commands, expected outputs, timings, machine used; how to try a NEW characteristic (falsification hook)
LICENSE              — MIT for code/scripts; CC-BY-4.0 for text/data (two files, stated in README)
CITATION.cff, VERSIONS.md (msolve 0.10.1: source tarball URL + sha256 + build flags; Lean toolchain; Mathlib commit from lake-manifest; Python/sympy versions), MANIFEST.sha256 (every file)
verify.sh            — `./verify.sh quick` (Lean builds + #print axioms + rank-one certificate + 2 sample charts per characteristic ≈ 10 min); `./verify.sh full` (every chart, every characteristic, both encoders ≈ hours); prints PASS/FAIL per CLAIMS.md line and exits non-zero on any FAIL
lean/                — K1695/*.lean (CyclicToMinpoly, CyclicVectorThree, TranspositionLemma(+Full), RankCriterion, StratumB, StratumBAssembly, RankToMinpoly, StratumBMinpoly, StratumBMinpolyGeneral, GoodCount3), lakefile.toml, lake-manifest.json, lean-toolchain, AXIOMS.txt (the recorded #print axioms), BUILD.md (one command: `lake build` + `lake env lean K1695/Axioms.lean`)
certificates/        — rank1/ (the rank-one ideals and bases: line encoder + codex N4x encoder); rank2/<family>/<p>/<chart>/ *.ms + *.gb for every unit terminal and non-unit prefix of both families (line: engine/harvest/k1695_r6_r2split_check (p=2), k1695_r6_r2split_odd/p{0,3,5,7,11,13,17,19,23,…}; codex: k1695_r6_r2split/, r2split357/, r2splitq/, r2split1113/); trees/ (the DPLL trees as JSON: assignment → unit/non-unit → children); controls/ (the realised-pattern control matrices with their case lists and NON-unit bases); if the raw .ms/.gb exceed a sane repo size, keep the .gb (small) + regeneration scripts and put the .ms in a release asset / Zenodo bundle with sha256s listed in MANIFEST
encoders/line/       — round6_r2split_check.py, round6_r2split_odd.py, round6_rank1_certify.py, round6_verify_cofactors.py, round6_controllable.py, run_capped.py — REFACTORED to relative paths and a single `--out` argument (they currently hardcode /Users/... paths: remove), plus a `run_decomposition.py --p P --chart K [--start-depth 6]` wrapper (the falsification hook: any reader can try characteristic 37 or a new chart order)
encoders/codex/      — r2split_encoder.py, r2split_search.py, r2split357_*.py, r2splitq_controls.py, verify_certificates.py, the N4x encoder — same refactor
data/                — inventories (cases per cycle type per characteristic), tree statistics tables (runs / unit / non-unit per p and chart), the exhaustive finite-field facts used as sanity (GL(4,2), GL(4,3), GL(4,4) hold 16.95; minimizer lists for (GC_4) as an appendix), counterexample matrices for the refuted conjectures (M for (Mono), C and T for (2Step), P_(24) for the face lemma) with verifiers
falsify/             — find_counterexample.py (random/structured search for an all-24-bad A over GF(p^k) with the exact cyclic oracle; documented as "should never find one") and try_new_characteristic.md
paper/               — a stub linking to automath-papers (the PDF/TeX live there); ABSTRACT.md
```
## Tasks
1. Assemble the tree above from the repository (paths in the registry §R6.42, §R6.78, §R6.81, §R6.86, §R6.91, §R6.93–§R6.103); coordinate with codex6's K6-PUBPKG output (engine/harvest/k1695_r6_pubpkg/package/) — reuse it, do not duplicate work; the GitHub layout is the canonical one.
2. Refactor all scripts to relative paths; pin versions; write REPRODUCE.md with the exact commands and the timings we measured (per chart 2–4 min; rank-one certificates 1–25 min per characteristic; Lean files 5–12 s each after Mathlib).
3. verify.sh quick must actually pass on this machine (run it once under run_capped; Lean part only when no other lake build runs) — record its output in VERIFY_QUICK.log.
4. CLAIMS.md must state precisely what is certified per characteristic and what is NOT (n ≥ 5 open; the "every field" claim only once §R6.104's (a)+(b) close — leave a clearly marked placeholder line to be flipped at publication).
5. Privacy grep (zero hits for the owner's name/email/machine paths), no engine transcripts, no cost/infra notes.
Deliver: the local repo (git init, author chy4pro, initial commit, NOT pushed), a tree listing, repo size, VERIFY_QUICK.log, DONE-github_pkg under engine/harvest/k1695_github_pkg/. Do not push, do not create the GitHub repo, do not post anywhere.
