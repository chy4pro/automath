# K6-R2UNIFORM — why does the SAME certificate tree close the rank-two stratum in every characteristic? Turn the computation into a proof for all fields (screen: sol; 0 heavy processes unless stated; light Python/sympy under run_capped 2.5 GB)
Facts (registry §R6.94–§R6.103; all engine/harvest/k1695_r6_r2split*/ and engine/harvest/k1695_r6_r2split_odd/): for A = I + UWᵀ with W = [I₂; w] and det U[{i,j},:] ≠ 0, the DPLL over the badness cases of the permutations (order: six transpositions, three double transpositions, eight 3-cycles, six 4-cycles; cases per permutation: E_r "rank(AP_σ − rI) ≤ 2" for each eigenvalue r of P_σ over the algebraic closure, N "∃t non-eigenvalue with χ_σ(t)I₂ + W_σᵀ adj(P_σ − tI)U = 0") closes with 0 open leaves in characteristics 0, 2, 3, 5, 7, 11, 13, 17, 19; for p ∈ {0, 7, 11, 13, 17, 19} the trees are IDENTICAL: per U-chart {1,2}: 827/828 msolve runs, 795 unit terminals; {1,3},{1,4},{2,3},{2,4}: 807/808 runs, 781 unit; {3,4}: 798/799 runs, 775 unit; max depth 10–12 (the nine (2,1,1)/(2,2) permutations plus one to three 3-cycles suffice; 4-cycles never needed). For p = 3, 5 the trees differ slightly (fewer eigenvalue cases). The unit terminals are ideals in ≤ 20 variables with ≤ 300 generators, each a Gröbner basis [1] computed by msolve in ≤ 12 s.
Goal: a proof valid over EVERY field (equivalently: the unit certificates exist over ℤ[1/N] for an explicit integer N, plus the finitely many primes dividing N handled separately — those below 23 are already done). Tasks (statuses PROVED / COMPUTED / CONJECTURED / FAILED): 1. Explain the characteristic independence: show that for each unit terminal S the Nullstellensatz certificate 1 = Σ h_i g_i can be chosen with integer coefficients up to a denominator N_S whose prime factors are ≤ 19 (or compute N_S: Singular `lift(I_S, ideal(1))` over ℚ on the terminal ideals — start with the smallest ones; a lift is memory-heavy (the line saw 2.5 GB exceeded on 21-variable systems), so measure first and report per-terminal feasibility; alternatively use msolve's `-P` or a tracer to get the reduction history). 2. Better: find the MATHEMATICAL reason. For a unit terminal, the case conditions of ≤ 12 permutations are inconsistent with det(I₂ + WᵀU) ≠ 0 and det U[rows] ≠ 0 — write the inconsistency as an explicit polynomial identity (e.g. the transposition E₁/E₋₁ conditions are linear-ish in u, w after the bordered-rank formula; maybe every terminal reduces to "some 2×2 minor of U or of I₂ + WᵀU is forced to vanish"). Prove such identities for a class of terminals by hand and let a script check that every terminal of the ℚ tree falls into a proven class. 3. If a uniform proof of the whole tree is out of reach, prove the theorem for all primes p ≥ P₀ by a bound: e.g. show the trees' unit certificates over ℚ have denominators dividing a computable N (Task 1) — then p ∤ N ⟹ characteristic p is covered, and the line runs the remaining primes individually. Deliver PROOF.md (+ scripts/logs) and DONE-r2uniform under engine/harvest/k1695_r6_r2uniform/. Never publish outward; nothing is SOLVED (n ≥ 5 is untouched by all of this).
