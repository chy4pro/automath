# K6-N5R1 — feasibility of the eigenvalue-case decomposition for the RANK-ONE stratum at n = 5 (screen: sol; ONE msolve under run_capped 2.5 GB; swap < 10 GB)
Context: at n = 4 the case decomposition (registry §R6.94–§R6.103) closed the rank-two stratum; the rank-ONE stratum at n = 5 is the natural first n = 5 target: A = I + uwᵀ, u, w ∈ F⁵ (10 variables), 119 non-identity permutations; for rank one, badness is only at EIGENVALUES of P_σ (a rank-one update cannot lower an invertible P_σ − tI to rank ≤ 3), so every case is an eigenvalue case E_r: all 4×4 minors of (P_σ + u w_σᵀ − rI) vanish (r a root of unity of order dividing a cycle length: 1; −1; ω, ω²; i, −i; ζ₅ powers — adjoin as variables with their cyclotomic relations when absent from GF(p)). Since 16.95 for n = 5 is verified exhaustively over GF(2) (registry), start with p = 3 and p = 0. Normalisation: scaling and conjugation give w₁ = 1 WLOG? (check: A ↦ QAQ⁻¹ permutes coordinates of u and w together; u ↦ cu, w ↦ c⁻¹w is free; so one nonzero coordinate of w can be set to 1 after a permutation — 9 variables) plus 1 + wᵀu ≠ 0 (variable z). Task: (1) build the encoder (own code; controls: the realised pattern of a concrete rank-one A over GF(3) must be non-unit; the identity is always bad); (2) DPLL over the permutations ordered by cycle type (transpositions (10), double transpositions (15), 3-cycles (20), (3,2) (20), 4-cycles (30), 5-cycles (24)), starting after all transpositions are assigned (2 or 3 cases each); measure per-node time/memory; report how deep the trees go and whether the characteristic-3 tree closes; (3) if it closes: "16.95 on the rank-one stratum at n = 5 in characteristic 3" (subject to the line's re-run); if it explodes: report where and why (which case combinations produce big ideals). DONE-n5r1. Never publish outward; nothing is SOLVED.
