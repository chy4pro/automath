# K6-R2HAND — Kourovka 16.95 on the rank-two stratum at n = 4, by hand (screen: sol; 0 heavy processes; light Python allowed under run_capped 2.5 GB)
Conventions: P_σ e_j = e_{σ(j)}; AP_σ has columns a_{σ(1)}, …; B cyclic ⟺ minpoly = charpoly ⟺ rank(B − tI) ≥ 3 for every t in the algebraic closure (n = 4).
Known (registry problems/k1695/campaign_registry.md): 16.95 at n = 4 is open only on the rank-two stratum: every all-bad A normalises to A = I + UWᵀ with U, W ∈ F^{4×2} (§R6.42, §R6.66); rank ≤ 1 is settled in characteristic 0 and every prime < 10⁴ by Gröbner certificates (§R6.42, §R6.78). WLOG rank W = 2 and, after conjugating by a permutation and changing the factorisation (R = (UG)(WG⁻ᵀ)ᵀ), W = [[1,0],[0,1],[w₃₁,w₃₂],[w₄₁,w₄₂]] (§R6.79). The identity is bad automatically. Rank-one update facts (Lemma T): for M of rank r, rank(M + xyᵀ) ≤ r iff x ∈ col M or y ∈ row M; for corank-1 M with kernels p (right), q (left) and an inner inverse M⁺: rank(M + xyᵀ) ≤ n − 2 iff qᵀx = 0, yᵀp = 0 and 1 + yᵀM⁺x = 0. A rank-TWO update UVᵀ can lower rank by 2: rank(M + UVᵀ) ≥ rank M − 2, so for the invertible M = P_σ − tI (t not an eigenvalue of P_σ) the perturbed matrix can still have rank 2.
Data: over GF(2), GF(3), GF(4) every invertible A has a good permutation (exhaustive: GL(4,2), GL(4,3), GL(4,4)); odd-characteristic rank-two families exist for which ALL SIX transpositions fail (e.g. a GF(7) Klein-four example, §R6.42 N4n) — so the witness must be a 3-cycle, a 4-cycle or a double transposition; exhaustive GF(3) low-rank data (engine/harvest/k1695_r6_gc4r2/): min g = 6 on the rank ≤ 2 stratum. The naive Gröbner encoding of "all 24 bad" (40 variables) exceeds 54 GB; the chart encoding (36 variables) is running on a VM.
Task (mark every statement PROVED / CONJECTURED / COMPUTED / FAILED): 1. PROVE: for every field F and all U, W ∈ F^{4×2} with det(I + UWᵀ) = det(I₂ + WᵀU) ≠ 0, some AP_σ is cyclic. Suggested organisation: (a) show the six 4-cycles cannot all be bad, or (b) show the eight 3-cycles cannot all be bad, or (c) mixed. For a 4-cycle σ, AP_σ = P_σ + U W_σᵀ with (W_σ)_{j,·} = W_{σ(j),·}; badness at t means rank(P_σ − tI + U W_σᵀ) ≤ 2; for t an eigenvalue of P_σ (4th roots of unity, or just 1 in characteristic 2) use the corank-1 criterion; for t not an eigenvalue use that rank(P_σ − tI + UW_σᵀ) ≤ 2 forces the 2×2 matrix I₂ + W_σᵀ(P_σ − tI)⁻¹U to be zero (Woodbury / matrix determinant lemma: rank drop by 2 iff that 2×2 matrix vanishes — prove it). This turns "σ bad at a non-eigenvalue t" into two-by-two polynomial equations in t whose solutions t must simultaneously exist for many σ. 2. Partial credit: characteristic 2 (only t = 1 is an eigenvalue of any P_σ); or the symmetric case W = U; or the sub-stratum where U has a zero row. 3. If you find a candidate all-bad A over some field, verify it with cyclic() for all 24 permutations and report it in full — do NOT interpret it; it would be a counterexample to 16.95 and goes through the line's disclosure protocol.
Deliver: PROOF.md with statuses, any scripts/logs, DONE-r2hand under engine/harvest/k1695_r6_r2hand/. Never publish outward; nothing is SOLVED.
