# K6-N5J — second family for the RANK-ONE stratum at n = 5 via the deflation (Krylov-determinant) ideal J₅ (screen: sol; ONE msolve at a time under run_capped --wall 900 --mem 2500000; swap gate < 10 GiB before every launch; NO Singular over the integers on this box)
Context (registry §R6.113): for A = I + u vᵀ ∈ GL(5,F) pick i with v_i ≠ 0 and scale so v_i = 1. Let R be A with row i
deleted; its columns are c_i = x (the other four coordinates of u) and c_k = e_k + y_k x for k ≠ i (y_k = the other
coordinates of v; e_k the standard basis of F⁴ after deleting coordinate i). For a column j of R and an ordering τ of the
other four columns put M = [c_τ(1) c_τ(2) c_τ(3) c_τ(4)] (4 × 4) and D_{j,τ} = det[c_j, M c_j, M² c_j, M³ c_j]. Deflation
(Lean `cyclic_standardBasis_of_principalBlock`, CyclicToMinpoly.lean:190, general n): D_{j,τ} ≠ 0 ⟹ e_i is a cyclic
vector of A P_σ for the permutation σ with σ(i) = j and σ ordering the other indices as τ ⟹ A P_σ cyclic. Therefore:
if the ideal J₅ ⊂ F[x₁..x₄, y₁..y₄] generated by ALL 5 · 24 = 120 determinants is the unit ideal, every rank-one A ∈ GL(5,F)
has a permutation matrix P with AP cyclic (16.95, rank-one stratum, n = 5). The line's own run: 74 nonzero determinants,
degree ≤ 8, msolve UNIT over GF(3) in 15 s (also over ℚ and small primes — see the registry for the current list).
Task (own code from THIS definition; do not read problems/k1695/round6_rank1_n5.py): (1) build the 120 determinants with
your own polynomial code (e.g. sympy or Singular over GF(p) only for polynomial arithmetic — no Gröbner bases over ℤ);
report how many are identically zero and the maximal degree; (2) msolve over GF(3), GF(2), GF(5), GF(7), ℚ, then every
prime p < 2000, one process at a time, and record for each the reduced basis ([1] or not) with the msolve version and
options; (3) controls that must NOT be [1]: (a) the ideal of only the determinants with j = i (x as the pivot column),
(b) the realised pattern: for a concrete rank-one A over GF(3) compute by exact linear algebra which (j, τ) give D ≠ 0 and
check that the ideal of the D's that vanish at that A is NOT unit; (c) the toy <xy, 1 − tx>; (4) an independent
finite audit: over GF(2) and GF(3) enumerate all rank-one A = I + u vᵀ ∈ GL(5,F) (up to the (u,v) ↦ (cu, c⁻¹v) scaling)
and verify by direct computation that some AP_σ is cyclic (this must agree with the registry's exhaustive GF(2) check);
(5) report: REPORT.md with a status ledger (COMPUTED / VERIFIED / FAILED), per-prime table, the exact msolve command
lines, timings and peak memory, and DONE-n5j. Never publish outward; nothing is SOLVED.
