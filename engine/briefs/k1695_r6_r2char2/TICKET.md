# K6-R2CHAR2 — Kourovka 16.95 at n = 4 in characteristic 2, rank-two stratum, by hand (screen: sol; 0 heavy processes; light Python under run_capped 2.5 GB)
Goal: prove that for every field F of characteristic 2 and every A = I + UWᵀ ∈ GL(4,F) with U, W ∈ F^{4×2}, some AP_σ is cyclic. Together with the certified rank-one stratum (registry §R6.42/§R6.78, all primes < 10⁴ incl. 2) and the normalisation (§R6.66: every all-bad A is, after scaling, I + rank ≤ 2), this would prove 16.95 for n = 4 over EVERY field of characteristic 2 — the statement the Gröbner runs (registry §R6.79/§R6.83, > 54 GB) could not reach. Conventions: P_σ e_j = e_{σ(j)}; AP_σ = P_σ + U W_σᵀ with (W_σ)_{j,·} = W_{σ(j),·}; B cyclic ⟺ rank(B − tI) ≥ 3 for all t in the algebraic closure.
Toolkit already PROVED (engine/harvest/k1695_r6_r2hand/PROOF.md — read it first): (L1) rank(M + UVᵀ) = n − 2 + rank(I₂ + VᵀM⁻¹U) for invertible M; (L2) for corank-one M: rank(M + UVᵀ) = n − 3 + rank K, K = [[0, qᵀU], [−Vᵀp, I₂ + VᵀM⁺U]]; (L3) for a 4-cycle P and t⁴ ≠ 1: (P − tI)⁻¹ = (P³ + tP² + t²P + t³I)/(1 − t⁴) so "bad at t" ⟺ E_P(t) := (1 − t⁴)I₂ + Wᵀ(I + tP³ + t²P² + t³P)U = 0; (L4) the support-pair theorem: A ≅ diag(B, I₂) always has a good σ. Characteristic 2 specifics: x⁴ − 1 = (x − 1)⁴, so the ONLY eigenvalue of a 4-cycle or a transposition or a double transposition is 1 (with corank 1 for a 4-cycle, corank 2 for a double transposition, corank 3 for a transposition? — compute: rank(P_τ − I) = 1 for a transposition, so corank 3; rank(P_(2,2) − I) = 2, corank 2); a 3-cycle has eigenvalues 1, ω, ω² (ω ∈ F₄); the (S′) data: 16.95 holds exhaustively over GF(2) and GF(4) (all matrices) and (OC₄) too.
Facts to use/prove: the identity is bad; for a transposition τ, rank(AP_τ − I) ≤ 1 + 2 = 3 — so transpositions CAN be good in rank two (unlike rank one); over GF(3) rank-two matrices exist with all 4-cycles bad and with all 3-cycles bad (§R6.85), but in characteristic 2 the 4-cycle analysis is simpler (single eigenvalue 1: badness at t = 1 is the corank-one criterion (L2) with p, q the all-ones-along-the-cycle vectors; badness at t ≠ 1 is (L3) with 1 − t⁴ = (1 + t)⁴ ≠ 0).
Task (mark every statement PROVED / CONJECTURED / COMPUTED / FAILED): 1. Prove the theorem in characteristic 2, or reduce it to an explicit finite check that you then perform (e.g. show that a counterexample would have to be defined over GF(2) or GF(4) up to conjugation, then use the exhaustive facts). 2. If the full theorem resists: prove it for U with a zero row, or for W = U, or for A with an eigenvalue of geometric multiplicity 2 in F itself (t = 1 for the identity is automatic). 3. Sanity-test every intermediate lemma over GF(2) and GF(4) with your own script (all (U, W) over GF(2): 2^16 pairs; a 10⁶ sample over GF(4)), with the controls: identity always bad; the GF(2) matrix rows 0001/0010/0111/1011 (= P_(14)(23) + vvᵀ, v = e₃+e₄) has g = 6.
Deliver: PROOF.md with statuses, scripts, logs, DONE-r2char2 under engine/harvest/k1695_r6_r2char2/. Never publish outward; nothing is SOLVED.
