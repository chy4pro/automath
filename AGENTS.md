# automath project instructions

<!-- global-memory:begin -->
## Global memory

Before a task, call global-memory `recall_presets` with project `automath` and the task type.
Relay a returned `user_notice` verbatim. Do not invent a notice if the call fails, and do not
save, delete or reclassify presets without user confirmation. Follow the global-memory skill.
<!-- global-memory:end -->

## Operating entry point

Read [notes/OPERATIONS.md](notes/OPERATIONS.md) before operating the research workflow.
It identifies the coordinator, actual work in progress, next gates and recovery status.
The earlier handoff is a dated snapshot, not the current ownership record.

- 2026-09-25: the owner restarted the project under a Claude (Fable 5.1) coordinator; the 09-09 Codex transfer is historical. Engines are Claude models only. See notes/STRATEGY_RESTART_20260925.md.
  The 09-09 handoff documents remain valid history; the legacy watchdog stays retired.
- Current user instructions and platform safety/agent limits override historical project notes.
- Old `ARCHITECTURE.md`, archived dispatches and the tail of `DIALOGUE_STATE_0829.md` contain
  obsolete fan-out, cloud and recovery instructions. Do not execute them as current work.
- Routine research reports go to the user in concise Chinese. Internal research notes and code
  are English. Report actual checks and costs, not model self-claims as verified results.
- Use one coordinator. Do not spawn additional agents without the applicable explicit authority.
  Existing worker seats must be inspected before any input; a completed report is not evidence
  that a still-running producer can safely be interrupted.
- Preserve exact quantifiers, ordered-pair/diagonal conventions, domains, coefficients and onsets.
  Separate finite checks, same-vendor review, cross-vendor review and kernel formalization.
- Never claim the original conjecture solved when only a bound or a restricted case was obtained.
  A numerical exponent with an unspecified coefficient is not a fully effective theorem.
- Do G2/source/priority checks before expensive new proof campaigns. No priority claim from an
  empty proof-claims page. No speculative fan-out or quota-burning work to keep seats occupied.
- GCP is retired. No paid cloud or new dependencies without the required authorization.
  Preserve Lean/Mathlib caches and the user's memory-protection process.
- No automatic community/Mathlib PRs or moderator messages. Do not act on old deletion queues.
  Publication gates and privacy checks are in OPERATIONS; project notes are not blanket permission
  to disclose internal material.
- A resumed session must check scheduler/worker state before dispatch. Do not claim unattended
  operation is restored unless the actual continuation mechanism has been tested and recorded.
